"""Bake-off sub-models for the possession engine (L2+). Each module here is a
pre-registered comparison, never a single hand-picked model; see
`docs/models/<model>/experiments.md` for the spec and `docs/models/README.md`
for the index.
"""

from __future__ import annotations

__all__: list[str] = ["pace"]
