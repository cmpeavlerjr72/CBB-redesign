# Change ledger (living doc — update statuses in place, never delete rows)

Purpose: the running list of every investigated change, its status, and the
evidence — so nothing goes dark. Statuses: SHIPPED / TESTING / BUILDING /
VALIDATED-PENDING-SHIP-ACTION / REFUTED / CONFIRMED-INCUMBENT / OPEN /
DEFERRED / BANKED. Rule: every status change updates this doc in the same
commit series as the change itself.

## A. Model changes (the cascade)

| change | status | evidence / waiting on |
|---|---|---|
| Control engine built to the pre-registered spec (counts x percentages, shared pace draw, fitted dispersion everywhere, OT stub) | BUILDING -> BUILT AND GATED 2026-09-10 | `docs/tests/control_engine_F2_2026-09-10.md`; F2 margin MAE 9.146, total MAE 13.204, Brier 0.1857 |
| Attempt counts drawn as four INDEPENDENT overdispersed marginals | REFUTED (as a distributional model) 2026-09-10 | G5 margin SD ratio 1.678, total 1.318, home/away score corr 0.084 vs 0.229, PIT p=2e-79. Residual corr of the counts is -0.56 / -0.36 / -0.29; independent components imply team-points SD 13.87 vs actual 9.22. Waiting on the L3 possession-outcome model. Report section 10, D1 |
| Pooled-season training with no season-level feature | CONFIRMED-DEFECT 2026-09-10 | G9 total bias -2.83 on F2 (-4.08 on F1) = 77% of the +3.69 season drift; close is unbiased at -0.56 on the same games. Fix is pre-registration of a season-aware level term at L2/L3. Report section 10, D2 |
| Overtime stub (5/40 of the game's own possession draw, re-simulated until untied) | OPEN (known gap, flagged in output) | Sim OT rate 0.0183 vs actual 0.0558; the stub also double-counts ~+0.16 poss/game because the pace target already includes OT. Waiting on the L5 overtime model. Report section 10, D5 |
| L2 pace bake-off: model class (multiplicative / ridge / Gaussian GLM / LightGBM) x feature set (A_tempo..D_plus_state) | DECIDED: `multiplicative`/`A_tempo` 2026-09-10 | F2 RMSE 4.8805 vs best raw arm 4.8177 (`lightgbm`/`B_plus_season`), inside the measured noise floor (0.157); every richer feature bundle and LightGBM land inside the same floor, so the zero-fitted-parameter formula wins the simplicity tie-break. `docs/models/pace/experiments.md` R2-R4 |
| L2 pace target definition: T_box vs T_pbp | DECIDED: T_pbp 2026-09-10 | Pooled 2022-2025 corr 0.9717, mean abs diff 0.854 poss (possession SD ~5.6); reliable, pre-registered default applies. `docs/models/pace/experiments.md` R1 |
| L2 pace distribution family: Gaussian fixed SD / heteroscedastic Gaussian / NegBin-Poisson count | DECIDED: `gaussian_hetero` (best-of-three, none pass PIT) 2026-09-10 | Every one of the 32 stage-1 arms fails the pre-registered PIT gate (F2 p < 1e-19 everywhere); traced to overtime games (~5.6% of games, +8.2 poss residual) plus residual excess kurtosis (~1.8) that persists even in regulation-only games. `docs/models/pace/experiments.md` R7-R8 |
| L3 possession event layer: CBBD pbp -> one row per possession and per chance | SHIPPED 2026-09-10 | `src/cbb_sim/pbp/{events,possessions}.py`, `scripts/build_possessions.py`, `data/processed/possessions/{possessions,chances}_{2022..2026}.parquet`. Validation: `docs/tests/possessions_build_2026-09-10.md` -- 68.7-69.4 poss/game vs the box formula's 67.2-68.2 (mean diff +1.01 to +1.53, SD 0.98-1.12, corr 0.982-0.986); segmentation-only score mismatch 0 team-rows in all five seasons; cross-source corr 0.973-0.994 against the L2 pace worker's independent hoopR count |
| L3 possession-outcome bake-off ROUND 1: multinomial ridge / GLM cascade / LightGBM / league-average baseline x A_team..D_plus_interactions x {first, cont} chances | NO ARM ADOPTED 2026-09-10 -- SUPERSEDED BY ROUND 2 (row below); the round-1 record stands as run | 0 of 11 arms pass both pre-registered gates on F2. Calibration fails on `FGA_jump2` alone (worst gated decile gap 2.06-2.95 pp vs a 2.00 pp gate on `first`; 7.3-12.4 pp on `cont`, which is the 2025 putback defect below). Every model arm PASSES responsiveness (4/4 monotone quintile steps, slope ratios 0.97 / 1.02 / 0.97 for 3PA / rim / TOV); the matchup-naive baseline fails it, as it should. Noise floor 0.000723 (`first`) from a 200-replicate game-block bootstrap; LightGBM+C_plus_state would have won on log loss by 0.0147, 20x the floor, had the gate passed. `docs/models/possession_outcome/experiments.md` section 3 |
| L3 possession-outcome bake-off ROUND 2: {ridge_logit, cascade, lgbm} x C_plus_state x training scheme {S0 static, S1 in-season walk-forward, S2 recency-weighted} x {first, cont}, over the corrected event layer and the `pbp_complete` universe | **DECIDED 2026-09-10: `lgbm` + `C_plus_state` + `S1` on `first`, `cascade` + `C_plus_state` + `S1` on `cont`** | Round 1 adopted nothing (0 of 11 arms passed both gates). Round 2 passes: 4 of 9 arms on `first`, 3 of 9 on `cont`. `first` winner F2 log loss 1.51543 with a worst gated decile gap of 0.98 pp against the 2.0 pp gate (round 1's best was 2.95 pp), beating the best gate-passing linear arm by +0.01576 against a noise floor of 0.000763. `cont` winner 1.49976 at 1.86 pp; no tree arm passed there. THREE separable causes, in order of size: (1) the rim override collapses the `cont` calibration failure from 7.3-12.4 pp to 1.70-6.70 pp; (2) the data fixes alone move `ridge_logit` on `first` from 2.06 pp FAIL to 1.94 pp PASS under the SAME static scheme; (3) S1 is what carries the tree arm across, 2.78 -> 0.98 pp for a log-loss gain of only +0.00136 -- the calibration gain is the result, not the loss. `docs/models/possession_outcome/experiments.md` sections 4-5 |
| TRAINING SCHEME as a first-class bake-off dimension: S0 static / S1 in-season walk-forward / S2 exponential recency weighting | DECIDED: **S1**, and it is now the DEFAULT FOR EVERY LATER SUB-MODEL 2026-09-10 | Pre-registered clause: 'if S1 or S2 wins, the same scheme is the default for every later sub-model unless its own bake-off says otherwise'. S1 wins both populations. S2 loses everywhere -- its F1-fitted half-lives come out at 365-730 days on every arm (i.e. almost no down-weighting) and it makes `lgbm` on `first` WORSE than S0 by +0.00067. Down-weighting old seasons is not the same operation as seeing the current one. OPERATIONAL COST, recorded because it is easy to lose: the deployed model is a SCHEDULE, refit at each month boundary of the live season; a stale artifact degrades toward S0, which fails the gate. `possession_outcome/experiments.md` section 5.5 |
| Season-level term (`season_idx` + `days_since_start`) as the fix for shot-mix drift at L3 | KEPT BUT INSUFFICIENT 2026-09-10 -- RESOLVED by the S1 training scheme (round 2), not by a better season term | On `first` the block clears the noise floor (F2 log-loss gain 0.001126 vs a floor of 0.000723) so it is KEPT, but it moves the worst gated calibration gap only 2.60 -> 2.13 pp against a 2.00 pp gate: it does not fix the level miss it was pre-registered to fix. On `cont` the gain is NEGATIVE (-0.002413) and the block is REJECTED. The first-chance two-point-jumper share is flat across the three training seasons (20.1 / 19.8 / 19.9%) and steps to 18.1% in 2025, so a linear season index has almost no trend to follow. Waiting on a pre-registered recency-weighting / season-random-effect / share-deviation reparametrisation. `possession_outcome/experiments.md` sections 3.4-3.5 |
| Game-state block (period, clock, score diff, bonus, transition) at L3 | KEPT 2026-09-10 | B_plus_season -> C_plus_state cuts F2 log loss by 0.1031 (`first`) and 0.0679 (`cont`), 30-140x the noise floor, in every model class. Explicit offence x defence interaction products move it by at most 3e-06, 240x BELOW the floor, and are REJECTED on both linear arms. `possession_outcome/experiments.md` sections 3.5 and 3.9 |
| L5 clock-consumption bake-off: empirical Kaplan-Meier state cells / heteroscedastic log-normal / heteroscedastic Gamma / discrete-time logistic hazard / LightGBM 9-quantile inverse-CDF x A_state..D_plus_season | NO ARM ADOPTED 2026-09-10 | 0 of 20 (arm x feature set) combinations pass the pre-registered F2 gates. Emergent G1: 0 pass; `gamma`/`C_plus_score` passes overall (mean +0.720, SD -0.280) then fails the all-powered-months clause 4/5 (January +1.119). PIT: 0 clean; `empirical` 5 leak-sized cells of 27 powered (worst D 0.083, all five the smallest powered cells), `lgbm_quantile` 6-7 (0.198), `hazard`/`gamma` 22-23 (~0.40), `lognormal` 26-27 (0.35). CRPS floor 0.006764 (worst game-block bootstrap SE; tree seed-refit floor 0.000295). `docs/models/clock/experiments.md` sections 2-4 |
| Parametric duration families (heteroscedastic Gamma and log-normal) and the logistic discrete-time hazard as the L5 predictive distribution | REFUTED 2026-09-10 | Their PIT failure reproduces IN-SAMPLE (`gamma`/`C_plus_score` pooled PIT K-S D 0.056 on its own training rows vs 0.0033 for the empirical arm), so it is a family failure, not overfitting or drift. A unimodal continuous law cannot carry the 1.90% point mass at 0 s, the shot-clock hump at 17-18 s and the thin tail past 35 s at once. A mixture or a grid-fitted discrete law is the replacement candidate. `clock/model.md` section 4.2 |
| Nine-quantile LightGBM inverse-CDF with linear interpolation as the L5 sampler | REFUTED (as a sampler; the point fit is the best in the grid) 2026-09-10 | Lowest F2 CRPS by 18x the floor (4.8082 vs 4.9307 for the best non-tree arm) AND the only arm that reproduces the end-of-period conditional mean (within 0.8 s in every band). But 7.0-7.6% of test rows get ZERO predictive mass on the duration that occurred (every other arm under 0.72%), the truncated tail costs 0.19-0.26 s of mean duration, and its emergent count is +2.19..+2.82 possessions per team -- the worst in the grid. More quantile levels or an explicit tail is the fix, and needs its own pre-registration. `clock/model.md` section 4.3 |
| End-of-period clock resolution at L5 | CONFIRMED-DEFECT 2026-09-10 -- the blocking defect | Actual mean duration falls 17.9 s (>90 s left) -> 15.6 (30-34) -> 13.1 (25-29) -> 10.7 (20-24) -> 7.4 (10-19) -> 3.1 (0-9). `empirical` is FLAT at ~9.5 s across the whole 0-34 range (its finest pre-registered bucket), `lognormal`/`gamma` flat at 15-17 s across all of it (linear in `seconds_remaining` through a log link), `hazard` bends half as far as it should; only `lgbm_quantile` tracks it. Consequence: 88.3% of real halves start their last possession under 35 s (mean 12.06 s) vs 88.9-99.7% and 5.8-11.8 s in sim. `clock/experiments.md` section 3.1 |
| Emergent pace (Decision 7) from a state-conditional i.i.d. possession-duration model | OPEN -- currently blocks G1 | Best emergent mean delta in the grid is +0.720 possessions per team (`gamma`/`C_plus_score`) against a +/-1.0 tolerance, but no arm holds it in every powered month. Every arm's overshoot is smallest in November and largest in January: real pace slows 1.83 possessions Nov->Jan, the chained sim slows 1.06. Same season-progress gap as L5 (the learning) and the L3 season-drift row, and the two should be pre-registered together. `clock/model.md` section 4.1 |
| Per-game count SD from i.i.d. conditional draws | NEARLY SUFFICIENT 2026-09-10 (0.2-0.3 possessions short) | Actual per-team count SD 5.088; i.i.d. renewal reference with no game-to-game pace variation 3.264; between-game spread the features carry 3.12-3.59; `gamma` achieves 4.81 (delta -0.28, inside the +/-0.75 tolerance). So dispersion is NOT the blocking half of G1. The residual is the L10 shape one layer down and a shared per-game pace shock is the candidate -- to be pre-registered, not assumed. `clock/experiments.md` section 3.2 |
| Emergent possession count is matchup-specific (CLAUDE.md responsiveness rule) | VALIDATED 2026-09-10 | All five arms slope with the pregame tempo-prior quintile in 4 of 4 steps, sim-vs-actual span ratios 0.79 / 0.92 / 1.01 / 0.98 / 1.06 (empirical / lognormal / gamma / hazard / lgbm); per-game sim-actual correlation 0.36-0.52. The L5 failure is a conditional-shape failure, not a matchup-signal failure. `clock/experiments.md` section 3.3 |
| L3 rebound bake-off: multinomial ridge / LightGBM / league-share-by-miss-type baseline x A_team..C_plus_state, 3 classes (OREB / DREB / dead ball) | DECIDED: `lgbm`/`C_plus_state` 2026-09-10 | The first L3 bake-off in which an arm passes BOTH pre-registered gates. F2 log loss 0.645565 with calibration 1.958 pp against a 2.00 pp gate and 4/4 monotone quintile steps on BOTH responsiveness drivers (slope ratios 0.9645 on the offence's as-of OREB% and 1.1008 on the defence's as-of DREB%). No LINEAR arm passes -- the best, `ridge_logit`/`C_plus_state` at 0.647533, misses calibration at 2.773 pp -- so the 'a tree must beat the best linear arm by more than the floor' clause has no passing linear arm to bind against; the raw gap is 0.001968 = 1.39x the floor, so the stricter reading selects the same arm. Noise floor 0.001412 (200-replicate game-block bootstrap; the LightGBM seed-refit SD is 6.7e-05). The matchup-naive baseline passes calibration and FAILS responsiveness, as it should. `docs/models/rebound/experiments.md` sections 3 and 6 |
| Is rebounding a LINEUP-level sub-model in the engine or a TEAM-level one? (`D_plus_lineup` vs `C_plus_state` on its own 2024/2025 fold) | DECIDED: TEAM-LEVEL 2026-09-10 | Fold L2 (train 2024, test 2025) on the 748,398 opportunities (95.25%) that carry all ten on-floor ids, C and D scored on exactly those rows. Adding the on-floor five's as-of individual OREB/DREB rates is worth 0.001373 log loss on `ridge_logit` (0.88x the fold's own block-bootstrap floor of 0.001557) and 0.001727 on `lgbm` (1.11x). The two STRADDLE the bar -- reported as such rather than rounded to a clean answer -- so the lineup aggregate is worth at most about one floor and does not justify a lineup-level sub-model. Neither C nor D passes the calibration gate on this fold anyway, and the fold is two seasons long (L13), which is exactly the resolution a one-floor effect needs. The individual-rate shrinkage strength was FITTED on the train season over [0, 50, 100, 200, 400] pseudo-opportunities (L13) and landed on 50. `docs/models/rebound/experiments.md` section 5 |
| Dead-ball rebounds as a third MODELLED class, or absorbed as a deterministic share by miss type | DECIDED: deterministic share by miss type 2026-09-10 | Dead balls are 0.46-0.92% of opportunities by miss type in the training fold. Replacing the three-class model with a LIVE-only binary model plus a fixed per-miss-type dead-ball share costs 0.000686 log loss = 0.49x the noise floor, i.e. inside it, and the composed model's dead-ball decile calibration is 0.421 pp. The class still has to EXIST -- it is invisible in the possession tables, where `DeadBallReb` is a no-op -- but it does not need a model. `docs/models/rebound/experiments.md` section 4 |
| L3 rebound S1 SCHEME CONFIRMATION (Decision 9c): `lgbm`/`C_plus_state` held fixed, refit calendar in {S0 static, S1-monthly, S1-conf-aligned, S1-weekly} | **DECIDED: `S1_weekly` 2026-09-10** | S0/F2 reproduces the adopted arm exactly (0.645565). All three S1 schemes beat the S0 reference on F2 log loss: monthly 0.644949 (9.2x floor), conf-aligned 0.644878 (10.3x), weekly 0.644522 (15.6x) -- `S1_weekly` wins as the only one within the floor of the best beater. FLOOR CORRECTION recorded in the same section: the pre-registered second-seed gap came back at an implausibly small 4e-06 (seeds 0/1 happen to sit close together); the operative floor is revised to round 1's own already-published 5-seed SD for this arm, 6.7e-05 (the larger, per the standing 'use the larger floor' convention) -- the winner is unchanged under the correction, now by 9-16x rather than 100-260x. CAVEAT, not smoothed over: unlike free throw's S1 confirmation (same date), NO scheme clears the 2.00 pp conf4 gate here (S0 2.462, monthly 2.660, conf-aligned 2.367 -- the best of the four but not the log-loss winner, weekly 2.565, a slight WORSENING vs S0). `S1_weekly` is selected on the primary metric alone, which the pre-registered OR rule permits; Decision 9's predicted first-four-conference-weeks damage is NOT resolved for this sub-model by any calendar tested and is carried forward as an open item. 29/23/6 refits (conf-aligned/weekly/monthly, F2 2025); F1 (2024) evidence exists only for S0 (cited) and monthly (refit 0.619726). Manifest: `data/processed/models/rebound/s1_confirm/S1_weekly/F2/manifest.json` (gitignored). `docs/models/rebound/experiments.md` sections 7-8, `docs/models/rebound/model.md` section 10 |
| L3 FT-1 trip structure: the rule-derived attempt counts checked against the data per season | VALIDATED 2026-09-10 | Foul class derived from CONTEXT ONLY (technical row / and-one signature / the fouling team's running period foul count), never from the attempt count, so the check is not circular. Violation rate 2.3857-2.7243% of trips in every season 2022-2026, dominated by one-attempt trips below the bonus threshold (~2,000 a season) where ESPN did not log the field-goal attempt. A further ~25,000 two-attempt trips a season are AMBIGUOUS rather than violations: once the bonus is in force the feed cannot separate a bonus trip from a two-shot shooting foul (data-defect row D5). `docs/models/free_throw/experiments.md` section 3.3 |
| The 2024-25 NCAA bonus-structure change the pre-registration flagged as a known era boundary | REFUTED 2026-09-10 -- the data shows no such boundary | Both thresholds re-derived from each season's OWN data (the share of one-attempt trips that missed, by the fouling team's prior period foul count) land at the same place in all five seasons: the one-and-one at the 7th team foul and the double bonus at the 10th, 2022 through 2026, 1 distinct threshold pair across the whole window. The free-throw growth L4 measured is VOLUME, not a rule: attempts per game 34.801 -> 40.834 while the one-and-one share of trips is flat at 28.327-27.451%. Table written to `data/processed/models/free_throw/bonus_era.json`, which the ENGINE reads into GameState. `docs/models/free_throw/experiments.md` section 3.1 |
| L3 FT-2 make-probability bake-off: team as-of floor / EB shrinkage / logistic ridge / LightGBM | DECIDED: `lgbm` 2026-09-10 | F2 log loss 0.575281 (Brier 0.194361), calibration 1.266 pp and 4/4 monotone quintile steps. It beats the best simpler PASSING arm, `eb_shrink` at 0.579314, by 0.004033 = 3.62x the noise floor (0.001113), so the simplicity tie-break does not fire. `ridge` fails calibration at 2.825 pp. `docs/models/free_throw/experiments.md` sections 4 and 6 |
| Free-throw make probability as a TEAM quantity (the pre-registered team-level floor) | REFUTED 2026-09-10 | `team_asof` is the worst arm on F2 by a wide margin (0.593953 against 0.575281) and fails calibration at 6.802 pp, of which 6.156 pp is SHAPE: a team rate cannot tell a 90% shooter from a 55% one, so its extreme deciles are far off even though it still slopes the right way (responsiveness passes, slope ratio 0.2981). L15's 'player identity dominates' reproduces here, at the free-throw line. The engine must carry shooter identity into free throws. `docs/models/free_throw/experiments.md` section 4 |
| L3 FT-2 S1 SCHEME CONFIRMATION (Decision 9c): `lgbm` held fixed, refit calendar in {S0 static, S1-monthly, S1-conf-aligned, S1-weekly} | **DECIDED: `S1_conf_aligned` 2026-09-10** | S0/F2 reproduces the adopted arm exactly (0.575281). All four schemes pass calibration and responsiveness and sit inside the noise floor on log loss (largest spread 0.000151 vs a 0.000147 second-seed floor) -- this is a CALIBRATION result, not a log-loss one, as L21 predicted one level down. `S1_conf_aligned` is the only scheme that beats the S0 reference beyond either threshold: the first-four-conference-weeks decile-calibration gap falls 1.271 -> 0.935 pp (0.336 pp beyond the pre-registered 0.25 pp bar) against `S1_monthly`'s 0.191 pp and `S1_weekly`'s 0.214 pp, both short. 29 refits (F2, 2025) vs monthly's 6 and weekly's 23. F1 (2024) evidence exists only for `S0` (cited from section 4, not refit) and `S1_monthly` (0.578044, worse than the cited F1 number 0.578489 by less than a floor) -- `S1_conf_aligned`/`S1_weekly` were not run on F1 by the pre-registered budget/drop order, so this is single-fold pending a second season. L24 segment (reported, not gated): 40.6% of the 2022-2024 training pool sits in an ft_trip_ambiguous-equivalent trip; clean-trip calibration is WORSE than overall for every scheme (3.27-3.78 pp vs 0.70-1.63 pp) and does not track scheme choice the way conf4 does -- flagged as an open question, not attributed. Manifest: `data/processed/models/free_throw/s1_confirm/S1_conf_aligned/F2/manifest.json` (gitignored). `docs/models/free_throw/experiments.md` sections 7-8, `docs/models/free_throw/model.md` section 10 |
| L4 usage bake-off: proportional / single-level Dirichlet / hierarchical Dirichlet / conditional logit / LightGBM, per event class | DECIDED: LightGBM on all five classes 2026-09-10, with one class UNCONFIRMED and two floor-thin | F1 (train 2024, test 2025) log loss against a 1.609438 uniform-over-five baseline: FGA_rim 1.502348 (16.9 floors clear), FGA_jump2 1.491034 (2.1), FGA_3 1.458927 (1.2), TOV 1.577664 (3.8), FT_trip 1.522266 (the only eligible arm). On the within-2025 robustness fold FGA_rim and FT_trip replicate (8.4 and 4.2 floors), FGA_jump2 and FGA_3 straddle the floor, and TOV REVERSES to 7.0 floors behind `proportional` -- recorded UNCONFIRMED rather than shipped. Fitted shrinkage (prior / pseudo on-floor events): FGA_rim position/50, FGA_jump2 league/50, FGA_3 position/25, TOV league/200, FT_trip position/200. `docs/models/usage/experiments.md` sections 3-6 |
| CFB's "too narrow" usage defect (per-player per-game count SD 0.54-0.89x real) | DOES NOT REPRODUCE IN CBB 2026-09-10 -- the CFB hierarchical Dirichlet is NOT adopted | Re-allocating the actual 2025 event sequence over the actual on-floor fives, the plain proportional allocator's SD ratio is 1.011 / 1.038 / 1.082 / 1.047 / 1.018 on the five classes against a 0.9-1.1 gate -- every class passes and every class errs WIDE, per role 0.948-1.175. Mechanism: CFB drew from ONE share vector per team-game so its only variance was multinomial noise around a fixed mean; here the allocation is conditioned on a five that changes between games and within a game, and that lineup variation already supplies the dispersion. Every fitted single-level concentration and every fitted between-role concentration came back at the grid's no-dispersion rung, so U2 is identical to U1 to six decimals and U3 costs log loss on four of five classes. The implementation is KEPT and tested because it is the right layer if a future rotation model under-disperses the lineups themselves. `docs/models/usage/experiments.md` R1, R4 |
| CFB's "too short" usage defect (5.2 rushers / 8.1 receivers with any share against 11.1 / 15.7 real) and its `extend_profile` fix | DOES NOT REPRODUCE IN CBB 2026-09-10 -- `extend_profile` is NOT ported | Players credited with at least one event per team-game, sim vs real: 7.090/6.825, 5.764/5.528, 6.820/6.689, 5.856/5.825, 5.372/5.092. Every class passes the +/- 0.5 gate and every class is sim-HIGH -- the tail is slightly too LONG, not too short, because the allocator cannot drop a player the rotation model put on the floor. `docs/models/usage/experiments.md` R2 |
| Every usage arm under-concentrates the TOP of the usage distribution | CONFIRMED-DEFECT, OPEN 2026-09-10 | The pre-registered top-1 / top-3 usage share check (+/- 2 pp) is the gate that actually binds, and it fails in the OPPOSITE direction from CFB's top-1 inflation: the sign is negative in 29 of 30 arm-class cells. Worst top-3 gaps: FT_trip proportional -2.95 pp, FGA_jump2 proportional -2.20, FGA_rim proportional -2.17, and on FT_trip the hierarchical Dirichlet (-2.18) and the conditional logit (-2.10) fail too, leaving the tree as the only eligible arm on that class. A shrunk as-of rate normalised over five is a smooth function of history and cannot represent "he is the man we run everything through in this matchup". Left OPEN rather than patched with a sharpening exponent, which is the post-hoc shape `SIM_GUARDRAILS` section 5 bans; the likely fix is an opponent- or matchup-conditioned share. `docs/models/usage/experiments.md` R3, `model.md` section 9 item 1 |
| Within-game usage concentration as a Polya-urn state (the Dirichlet conditioned on the game's own earlier events) | REFUTED as a source of signal 2026-09-10 | Reported as a labelled diagnostic only -- it is not a pregame quantity and never entered a decision. It improves log loss by 0.0000 to 0.0005 over the marginal on three classes and is WORSE on TOV and FT_trip, which is the same answer the fitted concentrations give from the other direction. `docs/models/usage/experiments.md` R5 |
| L3 FG-MAKE bake-off: team-level ridge / EB shooter x defence on the logit scale / logistic ridge / LightGBM, run SEPARATELY on the three shot classes | DECIDED for all 3 classes 2026-09-10 -- `lgbm`/`C_plus_state` everywhere after the FGA_3 RE-DECISION under `ARCHITECTURE_DECISIONS.md` Decision 8 | `FGA_rim`: `lgbm`/`C_plus_state` F2 log loss 0.641605, calibration 1.19 pp, both responsiveness drivers 4/4 monotone (slopes 1.008 shooter, 0.999 defence), beating the best passing non-tree arm (`ridge`, 0.659766) by 0.018161 = 33.7x the floor (0.000539). `FGA_jump2`: `lgbm` 0.642003, calibration 1.58 pp, 4/4 and 4/4 (0.986, 0.828), 32.4x the floor over `ridge`. `FGA_3`: only ONE of four arms passes both gates, the team-level baseline at 0.639094 -- 0.078 log loss and 92 floors WORSE than `lgbm` (0.561085), which passes calibration at 1.01 pp and the shooter driver at 4/4 / slope 0.988 and fails ONLY the defence driver at 3 of 4 steps. RE-DECIDED 2026-09-10 under Decision 8 (slope ratio in [0.8, 1.2] AND >= 3 of 4 steps, the 4-of-4 clause dropped below a 2 pp realised span): `FGA_3` -> `lgbm` at 0.561085, the only arm now passing, 45.0 floors clear of the best non-tree arm of any gate status. `FGA_rim` and `FGA_jump2` keep `lgbm`. Nothing was retrained -- the gate was re-applied to the stored run report by `scripts/train_fg_make_v1.py --redecide`. `docs/models/fg_make/experiments.md` sections 3, 9, 11.1 and 12 |
| The responsiveness gate's steps-only reading passes a model that is flat at the mean | CONFIRMED-DEFECT IN THE METRIC (not in any model) 2026-09-10 -> RESOLVED by `ARCHITECTURE_DECISIONS.md` Decision 8, same day | On `FGA_3` the adopted `team_baseline` passes the shooter-quintile monotone test with a predicted span of 0.31 pp against a realised 35.90 pp -- slope ratio 0.0086, 116x too flat, a model that cannot separate a 15% three-point shooter from a 51% one. `prob_metrics.quintile_responsiveness` predicted exactly this in its docstring ("flatness and wrong-direction are different failures"); the pre-registration gates on the steps and only REPORTS the slope. Under any reading that also gates the slope ratio, all three classes take `lgbm`. Not resolved by the worker after the fact: the finding went to the PM, who amended the gate GENERICALLY (Decision 8) rather than this model. Re-applied mechanically, the flat arm now fails on its own shooter slope and `FGA_3` changes to `lgbm`; `FGA_jump2`'s passing set also narrows from four arms to two (`eb_shrink` fails at a defence slope of 1.388, the over-steep side of the band). `docs/models/fg_make/model.md` section 4.3, `experiments.md` section 12 |
| A 4-of-4 monotone requirement on the defence's as-of three-point rate allowed | CONFIRMED TOO STRICT FOR THE SIGNAL 2026-09-10 -> SUPERSEDED by Decision 8 (the 4-of-4 clause is dropped when a driver's realised quintile span is below 2 pp) | The realised make rate across quintiles of that driver spans 1.37 pp (32.95 -> 34.32%), against 4.98 pp for `FGA_rim` and 3.33 pp for `FGA_jump2`. Both of the best-fitting arms slope the right way overall and dip at one interior step; the arms that clear 4/4 are the two that are near-monotone FUNCTIONS of the driver by construction. This is the "a defence barely controls opponent 3P%" result arriving on its own. `fg_make/experiments.md` section 11.1 |
| Field-goal make probability as a TEAM quantity (the pre-registered team-level floor) | REFUTED on ALL THREE classes 2026-09-10 (it was adopted on threes only under the superseded steps-only gate) | `team_baseline` is the worst arm on F2 in every class and FAILS calibration on `FGA_rim` (2.232 pp). The shooter block is worth 11.1 noise floors on `FGA_rim`, 46.5 on `FGA_3` and 3.0 on `FGA_jump2`, so L15's "player identity dominates" is a PER-SHOT-CLASS statement here, not a blanket one -- the two-point jumper is the shot where who takes it matters least, and its share has fallen 27.0 -> 23.6% of attempts since 2022. Under Decision 8 the team arm fails RESPONSIVENESS in all three classes on its own shooter slope (0.180 / 0.142 / 0.009), not merely calibration. `fg_make/experiments.md` sections 11.2 and 12 |
| Is the DEFENCE lineup-level or team-level for shot MAKING? (`D_plus_lineup` vs `C_plus_state` on its own 2024/2025 fold) | DECIDED: TEAM-LEVEL 2026-09-10 | Fold L2 (train 2024, test 2025) on the 1,202,616 attempts (97.31%) carrying all ten on-floor ids, C and D scored on exactly those rows. Adding the defensive five's as-of rim-protection and perimeter rates is worth 0.30 / -0.01 / 0.03 fold-floors on `ridge` (rim / jumper / three) and is NEGATIVE on `lgbm` in all three classes (-0.16 / -0.10 / -0.82). Not the rebound model's straddle -- a clean nothing. Defender-rate shrinkage FITTED on the train season over [0, 100, 250, 500, 1000] pseudo-attempts (L13), landing on 100. `fg_make/experiments.md` section 8 |
| G4 (implied team eFG%) read off the three adopted class models on the test season's own shot mix | PASSES on the pregame grouping, one oracle cell still out 2026-09-10 | RE-DECIDED trio (all `lgbm`): implied 50.932% vs actual 50.874% (gap 0.058 pp); terciles by pregame as-of form PASS on both sides (worst 0.71 pp offence, 0.91 pp defence against +/-1.0); terciles by REALISED eFG% (an oracle grouping, labelled) now PASS on offence (0.74 pp) and miss on defence (1.03 pp). The SUPERSEDED mixed trio (`lgbm`, `lgbm`, `team_baseline`) had a tighter level (0.002 pp) and a worse shape (oracle 1.21 / 1.09 pp): swapping the flat three-point arm out closes 0.46 pp of the offence oracle gap, which is the too-narrow-spread signature sourced in exactly that arm. `fg_make/experiments.md` sections 6, 10 and 12.4 |
| Scope of Decision 8 clause (a): is a driver with a sub-2 pp realised span exempt from the SLOPE band as well as from the step count? | RESOLVED 2026-09-10 -- PM wrote the scope into Decision 8: a sub-2 pp driver is exempt from BOTH clauses (a slope ratio over a noise-sized span is noise); `low_span_exempt` is the rule | Clause (b) exempts such a driver from the step count because "the steps are then noise"; clause (a) says the band applies to every driver and is silent on the exemption. It bites exactly once in this cascade so far: `FGA_3`'s defence driver spans 1.37 pp and LightGBM's slope on it is 0.474, so `strict` adopts NOTHING for that class while `low_span_exempt` adopts `lgbm`. Decision 8's own text states the outcome it expects (LightGBM), so `low_span_exempt` is what fg_make adopts and records; both readings are computed by `fg_make.decision8_verdict(..., reading=...)` and printed side by side in `fg_make/experiments.md` section 12, so neither is hidden. Every other class and every earlier model is unaffected -- no other adopted winner has a sub-2 pp driver. |
| L4 rotation bake-off round 1: minutes-share Dirichlet (R1) / hierarchical Dirichlet (R2) / per-possession stint hazard (R3) / empirical stint-sequence resampling (R4) | NO ARM ADOPTED 2026-09-10 | 0 of 4 arms eligible on F1 (train 2024, test 2025; 1,200 of 4,689 eligible games, 5 seeds, 86,687 rotation player-games). The pre-registered veto is the blowout + foul-trouble pair and **no arm clears the blowout band**: starters' share of the final 8:00 at \|margin\| > 15 is 0.4632 / 0.4722 / 0.5634 / 0.6050 against an actual 0.5237 (tol +/- 3 pp). Best on cells is R2 (4/6 G8 + 3/4 state). The two families fail in opposite directions: R1/R2 reproduce the minutes table and miss lineup concentration (top-1 five-man lineup share 0.231 / 0.238 vs 0.296, K-S D 0.23 / 0.19) and over-bench in blowouts; R4 reproduces lineup concentration inside seed noise (K-S D 0.036) and misses the minutes table and the close bands. Noise floor 50 seeds x 150 games: every gap is 3-10x the seed SD. `docs/models/rotation/experiments.md` section 2 |
| L4 rotation bake-off round 2: R2 incumbent / R5 donor-sequence + fitted state overrides / R6 respecified stint hazard | NO ARM ADOPTED 2026-09-10 | 0 of 3 arms eligible on F1 (1,600 of 4,689 eligible games, subset seed 2025, 5 seeds, 115,702 rotation player-games). Round 2's veto needs all four state cells: R2 3/4 (blowout -4.7 pp), R5 2/4 (close bands -4.3 / -6.9 pp), R6 2/4 (close bands -6.5 / -7.2 pp). The arms did what they were specified to do -- R5's lineup concentration is the best of all six arms (top-1 lineup share 0.2876 vs 0.2940, K-S D **0.029**) and its blowout band lands at -2.9 pp, and R6's interactions give the hazard the blowout response R3 could not represent (+2.0 pp vs R3's +4.0) -- but neither holds the close-game end while doing it. `experiments.md` section 4; QUALIFIED by the hazard-matrix defect below |
| L4 rotation bake-off round 3: corrected R5 / R7 fitted logistic close-game keep-starters override / R8 fitted per-cell keep threshold, incumbent R2 | NO ARM ADOPTED 2026-09-10 | 0 of 4 arms eligible on F1 (the SAME 1,600-game subset as round 2, subset seed 2025, 5 seeds, 115,702 rotation player-games). State cells (actual 0.7491 / 0.7240 / 0.5223 / 0.4613): R2 0.7281 / 0.6943 / **0.4750** / 0.4648 (3/4, fails only the blowout band at -4.7 pp and reproduces its round-1 and round-2 numbers to four decimals for the third time); R7 0.7074 / 0.6749 / 0.5994 / 0.5125 (0/4); R5 0.6942 / 0.6566 / 0.5911 / 0.4964 (0/4); R8 0.6982 / 0.6599 / 0.5956 / 0.4997 (0/4). **The close-game cell is unreachable by the keep family at any knob setting** -- the largest close-band share anywhere in R7's 24-point grid is 0.6994 against a 0.7110 target, and raising the keep scale or base rate LOWERS it, because the fitted starter-vs-bench separation in the close-and-late state is only ~1.0 in log odds and any base rate large enough to keep starters also keeps bench players who then displace un-kept starters. R7's +1.3 / +1.8 pp gain on the close bands does not clear the spec-identical-refit floor (1.5 / 1.4 pp). Banked: R7 has the best lineup concentration of any arm in three rounds (top-1 five-man share 0.2888 vs 0.2940, K-S D **0.0302**; per-player minutes K-S 0.0782) and every arm's quintile slope against the pregame team prior is right (actual +0.692, R2 +0.645, R5 +0.742, R8 +0.728, R7 +0.826), so all four failures are level failures, not responsiveness failures. `docs/models/rotation/experiments.md` sections 6-7, `docs/tests/rotation_close_game_audit_2026-09-10.md` |
| L4 rotation ROUND 3b: S1 (in-season monthly walk-forward refit, L21) vs the static scheme rounds 1-3 used | **S1 ADOPTED as the rotation model's training scheme** 2026-09-10 (it adopts no MODEL -- round 3 adopted none) | Six windows on the same 1,600-game universe, 3 seeds, the same blind grading path. No gate cell regressed beyond round 3's noise floor A; six improved beyond it and every one moved toward the actual: R7's blowout band 0.5994 -> 0.5679 (-3.15 pp vs a 1.21 pp floor, still a FAIL at +4.6 pp), and R2's top-5 share 0.7683 -> 0.7630, top-8 0.9656 -> 0.9626, players > 0 min 9.039 -> 9.129, minutes mean 25.55 -> 25.36. Three verdicts flipped and all three are cells sitting on the tolerance line, not scheme effects: **R2's 6-15 band was -2.97 pp against a -3.00 pp tolerance under the static fit and flips to FAIL on a 0.29 pp move (floor 1.06 pp), so R2 is 2 of 4 state cells under S1 and its `3 of 4` headline from rounds 1-3 is boundary-thin**; R2's top-5 share and R5's foul-trouble cell flip the other way on similarly sub-floor moves. Second finding: the override knobs are NOT IDENTIFIED -- R7's keep scale spans 0.0-2.0 and `q0` 0.02-0.15 across six windows of one season, R8's theta 0.1-1.0, matching the instability noise floor B found between two static refits. Refit cost 7.7-13.0 min per window on four threads. Parameter sets persisted as `rotation_fit_v3_S1_{YYYYMM}.json` by window START month; `rotation_fit.json` (what the engine reads) untouched. `docs/models/rotation/experiments.md` sections 8-9 |
| L4 rotation bake-off ROUND 4, a NEW MODEL FAMILY: per-player discrete-time substitution hazards (H1 hard second-half reset / H2 reset must be earned / H3 LightGBM hazard) against the incumbent R2, static and under S1 | **NO ARM ADOPTED** 2026-09-11 -- but the family produces both structural facts three rounds could not, and the failure is localised to one measured mechanism | 0 of 4 arms eligible on F1 (the SAME 1,600-game subset as rounds 2/3/3b, subset seed 2025, 3 seeds under S1 + 3 static, 9,600 simulated team-games and 68,859 rotation player-games per arm). The veto is now EIGHT state cells: the four from round 3 plus three second-half-TIP bands and the opening ten minutes of a close game, both pre-registered from `docs/tests/rotation_sub_hazard_audit_2026-09-10.md`. **What the family fixes.** H2 tip (actual 0.9678 / 0.9611 / 0.9563): R2 0.7892 / 0.7977 / 0.7338 (-17.9 / -16.3 / -22.3 pp), H1 **0.9390 / 0.9374 / 0.9434** (all PASS), H3 0.9394 / 0.9418 / 0.9453 (all PASS). Opening ten minutes at |m| <= 5 (actual 0.7822): R2 0.5988 (-18.3 pp), H1 0.7434 (-3.9), H3 **0.7581** (PASS). Blowout band (actual 0.5223), which R2 has failed in all four rounds (-4.7 / -4.7 / -4.7 / -5.3 pp): H1 **0.5446** / H2 0.5471 / H3 0.5517, all PASS for the first time. Within-player minutes SD ratio, which had FAILED for every arm in every round (1.05-1.35): H1 **1.079** / H2 1.080 / H3 1.022, all PASS -- the CFB narrow/wide pair closes. Per-player minutes MAE (the pre-registered primary metric): R2 9.794, H1 **8.819**, H2 8.897, H3 **8.674**, against a floor-A seed SD of 0.0147, i.e. 61-76 noise floors; no arm in three rounds had beaten R2 on per-player minutes at all. **What blocks adoption.** The close-and-late band (actual 0.7491): H1 0.6692 (-8.0 pp) against R2's 0.7254 (-2.4 pp, PASS). The mechanism is measured, not guessed: the hazard arms substitute **43% too often** (0.2058 at a possession boundary against a real 0.1518 and R2's 0.1437) and run 19.7-20.4 distinct lineups per team-game against 14.84, which compresses the late-game spread across margin bands to 12.4 pp against a real 22.7 pp. Same defect from the other side: **the reset must be GIVEN, not earned** -- H2 lands at 0.848-0.855 at the tip while the fitted boundary hazards are right to 1 pp on the training season (bench exit 0.786 predicted vs 0.791 actual; starter entry 0.771 vs 0.780), because independent Bernoulli exits capped at the bench size realise only about four fifths of a coordinated two- or three-player swap. **L25 reachability, computed before the pre-registration**: the saturated cell form of the family, fitted on 2024 and run on 400 2025 games under the five-on-the-floor constraint, hits all seven target cells within 1.0 pp (close-late 0.7393 vs 0.7388; H2 tip 0.9758 / 0.9670 / 0.9525 vs 0.9732 / 0.9574 / 0.9508), against the override family's reachable MAXIMUM of 0.6994 on the close band. **Identification**: round 4 has no knobs, and a spec-identical refit under a second seed moves six of eight cells by 0.04-0.81 pp -- the opposite of round 3's finding that the override knobs were not identified. Decision 8 satisfied by all four arms (slopes +0.629 / +0.628 / +0.672 / +0.706 against an actual +0.692, monotone 4 of 4) and separates none. H3 is additionally not expressible in the sim loop (a live model call, which CLAUDE.md bans). Round 5's arm, named by the evidence: this family with a **joint dead-ball substitution draw** (one Bernoulli per team per boundary for a substitution wave, conditioned on `prev_end`, then a size and a composition), not a third family. `docs/models/rotation/experiments.md` sections 10-11, `docs/tests/rotation_sub_hazard_audit_2026-09-10.md` |
| L5 clock ROUND 2: finer period-end state (ten seconds-remaining buckets x period type x trailing/tied/leading, last-shot and two-for-one windows) x 6 arms incl. a two-regime composite | NO ARM ADOPTED 2026-09-10 | 0 of 6 pass the three pre-registered gates on F2 (emergent G1, PIT by cell, end-of-half now a gate). The state change IMPROVES F2 CRPS for every non-tree arm by 9-45x the 0.006753 floor (`empirical` 4.9307->4.8698, `gamma` 5.0394->4.9035, `hazard` 5.0294->4.9505, `lognormal` 5.2002->5.0590) and leaves the tree unmoved (4.8082->4.8091), yet makes the EMERGENT COUNT WORSE on four of five round-1 arms (`gamma` +0.720->+1.531, `empirical` +1.124->+1.669, `hazard` +1.810->+2.598, `lgbm` +2.190->+2.525). `docs/models/clock/experiments.md` sections 6-7 |
| Conditional duration given the clock is TRUNCATED BY THE HORN, so a correctly-calibrated model subdivides the end of a half | CONFIRMED-DEFECT AND FIXED 2026-09-10 (round 3); NOT SUFFICIENT | Round-3 paired controls (same code, same seed, flag is the only difference): empirical G1-CC +1.805 -> +1.357 and EOH duration -5.28 s -> -3.44 s; gamma +1.531 -> +1.130 and -3.51 s -> -1.85 s. The corrected flag is `end_clock <= 0` (0.6751% of rows, 3.1x the old `end_period` flag; 63.2% of possessions starting under 5 s). Mechanism confirmed directly: predicted INTENDED duration in the 0-4 s band goes 1.7 s (old) -> 8.8 s / 12.6 s (corrected) while the 90+ s band is unchanged to 0.2 s. Worth ~0.45 possessions per team-game; +0.9 to +1.4 remain. `clock/experiments.md` sections 8-9, `clock/model.md` section 11 |
| The pre-registered L5 end-of-half statistic is contaminated by CBBD feed truncation | CONFIRMED-DEFECT (in the GRADING TRUTH, not the model) 2026-09-10 | Only 44.6% of 2025 halves have a last logged possession ending at 0:00; the median unaccounted time is 1 s, the 90th percentile 17 s, the mean 5.29 s. The 11.7% of halves whose last logged possession starts with 35+ s left have a mean END clock of 20.4 s -- the feed simply stops. On clock-complete halves the actual share is 0.9556, not 0.8832, and `gamma` (-0.009) and `hazard` (+0.003) land INSIDE the 0.00634 floor. The gate is reported as pre-registered AND re-read on the clean sub-universe; it is not re-based. A CBBD-side clock-completeness flag belongs in `games_universe`. `clock/experiments.md` section 6.3 |
| Two-regime arm (round-1 tree above T seconds, fine empirical table at or below T; T chosen on F1) | REFUTED 2026-09-10 | T is flat across {30,45,60,90} on F1 (score 48.2-50.5, T=45 selected), F2 CRPS 4.8192 is 0.010 WORSE than the plain tree, and its emergent mean +2.650 is the worst in the grid. Its behaviour is dominated by the upper regime, which is the round-1 model. `clock/experiments.md` section 6 |
| LightGBM tree-parameter search for L5 (num_leaves x min_data_in_leaf, F1 only) | NO EFFECT 2026-09-10 | F1 CRPS 4.76850 / 4.76611 / 4.76803 across the complexity ladder -- a 0.0024 spread inside the 0.006753 floor -- and the F1-chosen pair is round 1's own default {63, 500}. Round 1's fixed hyperparameters were not what held the tree arm back |
| L4 attribution bake-off: proportional / conditional logit / LightGBM per choice target, and team ridge / aware ridge / LightGBM per binary | **DECIDED 2026-09-10 (bake-off complete)** | Eight targets (`REB_off`, `REB_def`, `assist` over FOUR alternatives, `steal`, `block`, plus the binaries `assisted` / `stolen` / `blocked`) on F1 (train 2024, test 2025) and the within-2025 robustness fold, against uniform baselines of 1.609438 over five and 1.386294 over four. Full pre-registered grid (3 LightGBM rungs, 3 F1 seed refits, 40 game-level draws, 200 bootstrap reps), wall time 682.4s, no runtime lever needed. F1 winners: `REB_off`->cond_logit (1.431192), `REB_def`->lgbm (1.559167), `steal`->proportional (1.586249), `block`->lgbm (1.273376), `assisted`->aware_ridge (0.558057), `blocked`->aware_ridge (0.264337); `assist` and `stolen` adopt NO WINNER on F1 (`assist`'s best arm, lgbm, fails the top1/top3 game-level check; `stolen` fails calibration on every arm). `assist` recovers `lgbm` as a winner on the within-2025 fold; `block` loses its winner there (every arm fails calibration on a 17,234-row test set, flagged underpowered rather than a reversal); `stolen` fails on both folds -- the one clean "not usably modellable with this feature set" result in the bake-off. Every eligible arm on every target passes the Decision-8 responsiveness gate at 4/4 monotone steps. 27 tests passing throughout. `docs/models/attribution/experiments.md` section 3, `docs/models/attribution/model.md` section 4 |
| `build_team_asof`'s league-as-of-rate columns were silently NaN/zero for all three L4 binaries (`assisted`/`stolen`/`blocked`) | CONFIRMED-DEFECT, FIXED 2026-09-10 | The function accumulated BOTH seasons' six (population x side) count groups into one flat list, then merged all of them sequentially; since 2024's and 2025's groups reuse the same column names (`made`, `ast`, ...), pandas silently suffixed every one `_x`/`_y`, the bare names never existed, a defensive `if c not in tg.columns: tg[c] = 0.0` fallback manufactured an all-zero column for literally every count (not just the league rate -- the per-team num/den were silently zero too, the more dangerous half of the bug), and `_safe_div(0, 0)` came out NaN on every row for all three binaries -- exactly why the interrupted 2026-09-10 16:50 ET launch crashed on them with `ValueError: Input X contains NaN`. Fixed by merging each season's six groups column-wise (unique names within a season) and concatenating the two seasons row-wise; no other line changed. Verified: rebuilt `team_asof_v2.parquet` has real per-team variation (e.g. `off_assisted_lg` mean 0.5119, matching the reported base rate) where every row was previously NaN; `pytest tests/test_attribution.py -q` unaffected (27 passed before and after). `docs/tests/attribution_team_asof_lg_defect_2026-09-10.md` |
| L4 usage ROUND 2 (data fix): field-goal shooter keyed on `shot_shooter_id` instead of `participant_1_id`, identical pre-registration otherwise | **RE-RUN COMPLETE 2026-09-10 -- WINNER UNCHANGED ON ALL FIVE CLASSES; awaiting PM adoption of the v2 artifacts** | Round 1's shooter label was the ASSISTER on 48.98% of assisted made FGAs, mislabelling 11.9/5.0/14.1% of `FGA_rim`/`FGA_jump2`/`FGA_3` rows in 2024 and 12.0/4.9/14.1% in 2025 (audit: `docs/tests/shooter_key_audit_2026-09-10.md`). Re-ran the identical spec with only the label changed (`scripts/train_usage_v2.py` IMPORTS `train_usage_v1`, so 'identical apart from the label' is enforced by construction). F1 (selection fold) winner is `lgbm` on all five classes in BOTH rounds; margins 16.9->11.0, 2.1->1.9, 1.2->1.5, 3.8->3.9 floors, `FT_trip` the only eligible arm in both. What changed: U1/U2 become ELIGIBLE on `FGA_rim` (top-3 usage-share gap -2.17 -> **-1.79 pp** against a 2.0 pp gate) because credit returns from the passer to the finisher; `hier_dirichlet` loses eligibility on `FGA_jump2` (-2.20 -> -2.29 pp, the least-assisted class, 0.09 pp = inside Monte-Carlo variation). `TOV` and `FT_trip` are byte-comparable, as pre-registered (their labels were never affected). Seed noise 1.0e-05..6.8e-05 vs bootstrap floors 0.000799..0.001656. Drop for missing shooter id: 1,136 rows, 0.04-0.27% per class, never imputed. Artifacts in `data/processed/models/usage_v2/` (round-1 artifacts untouched -- the engine reads them concurrently). `docs/models/usage/experiments.md` section 8 |
| L3 FG-MAKE **ROUND 2**: state parametrisation, five arms over a fixed LightGBM class and fixed round-1 parameters (S-A round-1 leaked reference / S-B no state / S-C engine-safe state, no margin / S-D + three evidence-fixed saturating indicators / S-E + corrected continuous margin) | **DECIDED 2026-09-10: S-C `R2_C_safe_state` on ALL THREE classes; artifacts marked adopted, engine default UNCHANGED pending the PM's switch** | Pre-registered in `fg_make/experiments.md` section 13 and committed (5e54872) BEFORE the round ran; results in section 14. S-A reproduces round 1 bit-identically (delta 0.0 on all three classes), so the harness is its own control. F2 log loss / floor: `FGA_rim` S-E 0.660026 < S-D 0.660124 < S-C 0.660471 < S-B 0.667788 against a 0.000539 floor -- the first two gaps are 0.18 and 0.64 floors so the tie-break takes the simpler arm, and S-C beats S-B by 13.57 floors; `FGA_jump2` S-B fails calibration (2.014 pp) and S-C is both lowest (0.664094) and simplest; `FGA_3` S-B and S-E fail Decision 8 on the sub-2-pp `def_allow_c` driver (2 of 3 required steps) and S-D beats S-C by 0.09 floors, inside it. S-C wins under BOTH readings of Decision 8's low-span clause. Calibration improves on all three classes (1.186/1.580/1.007 -> 0.663/1.212/0.852 pp). COST, stated plainly: S-C is **35.0 / 38.2 / 36.4 noise floors WORSE on log loss** than the leaked S-A -- that gap WAS the leak. `fg_make/experiments.md` section 14 |
| The Decision-10 CLOSED-LOOP gate, first application (fg_make round 2) | **PASSED by S-B, S-C, S-D, S-E; FAILED by the round-1 arm 2026-09-10** | 500 games x 5 seeds per arm, paired streams, `ENGINE_EVENT=round2_s1` / `ENGINE_CLOCK=reference` / `ENGINE_ROTATION=reference`, gate stated RELATIVE to S-B (no margin anywhere, hence no loop by construction). S-A: margin SD **35.389 vs 14.128** (+21.26, CL1 FAIL), home/away score correlation **-0.605 vs +0.245** (CL2 FAIL), total bias -3.06 relative (CL4 FAIL) -- L23's signature reproduced on an independent subset. S-C/S-D/S-E move margin SD by at most 0.079, correlation by 0.028, possessions by 0.075 and total by 0.66: indistinguishable from the no-state reference. The residual absolute misses (possessions +3.3, PPP -0.022, margin SD 14.05 vs a realised 12.13) are identical across all four honest arms INCLUDING the one with no state at all, so they are the clock's (L20, L23), not this model's. `fg_make/experiments.md` section 14.5, `data/processed/models/fg_make/round2/closed_loop.json` |
| L5 clock ROUND 3c: the Decision-10 CLOSED-LOOP run that decides the state parametrisation (gamma_aft and empirical_km3_srfloor x P1/P2/P3, S1 monthly schedules, inside the engine) | **NO ARM ADOPTED 2026-09-11** | 500-game model-blind stride of the F2 2025 slate x 25 seeds per arm, paired streams, ALL 19 runs on the engine loop at commit `6431772` (six were re-run after bit-identical reproduction showed they predated it), every other sub-model pinned by explicit env value (`ENGINE_EVENT=round2_s1`, `ENGINE_FG_MAKE=round2b_S_C_s1`, `ENGINE_FG3=decision8`, `ENGINE_ROTATION=reference`). 0 of 6 pass the G1 MEAN on either game set; all 6 pass the G1 SD. Best arm the project has produced inside the engine is `empirical_km3_srfloor|P3|S1` at **+1.161** clock-complete / +1.666 all against the incumbent's +2.743 / +3.443, floor 0.135 -- every candidate is 1.3 to 1.6 possessions better than the incumbent, 10 to 12 floors wide. Criterion 2 (margin SD and home/away correlation within the floor of the incumbent) is SATISFIED by `gamma|P3` and half-satisfied by the two cell arms; it was not softened and nothing was adopted under it. `docs/models/clock/experiments.md` sections 12-14, `docs/tests/clock_round3c_closed_loop_2026-09-10.md` |
| Clock state parametrisation P1 (margin live) vs P2 (margin deleted) vs P3 (engine-safe end-game indicators) | **DECIDED 2026-09-11: P2 and P3 TIE, both beat P1 by 0.42-0.43 possessions** | The question round 3b proved the offline chain could not answer (it replays the real score sequence, so it holds no loop). In the engine, 25 seeds, G1-CC: P1 +1.876, P2 +1.458, P3 +1.446 against a 0.135 floor -- P2 vs P3 is 0.012 apart, a tie that the pre-registered simplicity order gives to P2, though P3 is clearly better on the end-of-half duration (-0.138 s vs -0.630 s), so the tie is on the aggregate and not on the mechanism. Offline the same contrasts read 0.038 (P1->P2) and 0.227 (P1->P3), so the engine prices the loop at ~2x what information alone showed and 10x what deleting the feature offline showed. On the cell-based arm the P1->P3 gap (0.082) is inside the floor. Neither arm is adopted; the state question is closed and round 4 is a duration-LEVEL question. `clock/experiments.md` sections 13.3 and 14.2 |
| A Decision-10 FREEZE ablation as a MEASUREMENT of a feedback loop | **REFUTED as a measurement, CONFIRMED as a detector 2026-09-11** | Holding the clock's margin at zero for a model trained WITH it swings possessions 4.468 and the game total 9.9 points, landing 2.6 possessions and 9.4 points BELOW actual; refitting the same family without the margin (P2) swings 0.418. Cause: `score_diff = 0` with forty seconds left is a tie game, whose fitted regime is play-normally, so the frozen model stops producing the short intentional-foul possessions that end close games -- its mean last-possession duration is 15.34 s against an actual 11.89. Both a freeze and a refit-without-the-feature arm must run before a magnitude is quoted. L23 stands (fg_make's five refit state arms agreed with its freeze); the "+3.8 possessions of clock loop" quoted from that ablation is an overstatement of what a refit recovers, and the honest engine figure is 0.42. `clock/experiments.md` section 13.4 |
| A long paired ENGINE design can straddle a concurrent change to `loop.py` and look fine | **CAUGHT AND REPAIRED 2026-09-11, before any of it was read as evidence** | Rotation round 4's commit `6431772` moved `push_lineups()` after the period/halftime block and added a `rotation_sub` RNG family, mid-way through round 3c's four-hour run sequence. Commit timestamps cannot bound a working-tree edit, so the design was bisected by BIT-IDENTICAL REPRODUCTION: re-running `clock3c_incumbent_s5` (21:52 EDT) on current code differed on 1286 of 2500 rows, while `gamma_P1_s5` (22:49) and `srfloor_P3_s5` (22:59) reproduced exactly -- placing the change inside this worker's own idle window (22:23-22:45, waiting on the S1 trainer). The six phase-1 runs were deleted and re-run; all 19 are now on one loop. Nothing moved by more than 0.05 possessions and no verdict changed, but that was timing, not a control. Two cheap controls follow: `run_meta.json` should record the ENGINE COMMIT HASH, and a long run sequence should re-run its earliest arm against its latest code and assert bit-identity before the table is read. `clock/experiments.md` section 14, `docs/tests/clock_round3c_closed_loop_2026-09-10.md` section 11 |
| Emergent pace (Decision 7): what is LEFT of the G1 miss once censoring and the state loop are handled | **LOCALISED 2026-09-11 -- a uniform duration LEVEL bias, not an end-of-period or feedback one** | Actual mean regulation possession duration on the round-3c subset is 17.555 s on clock-complete games (21,722 possessions); the best arm produces 17.216 s, -0.339 s = -1.93%, which implies +1.35 possessions against an observed +1.161 (gamma P3: 17.166 s, -2.22%, implies +1.55 vs +1.446; incumbent 16.829 s, -4.14%, implies +2.95 vs +2.743). Ruled out by the same run: the horn (last-possession duration now within 0.14-0.72 s of actual, against the incumbent's -5.97 s), the margin loop (0.42-0.43, and removing it entirely leaves +1.46), binning (cell arms are lookup tables by construction), responsiveness (best arm's tempo-quintile slope ratio 1.061, inside Decision 8's band). Round-4 candidates: dead-ball / administrative time the segmentation merges into ordinary possessions, and the OT gap (sim 2.6-4.2% vs actual 6.8%). `docs/tests/clock_round3c_closed_loop_2026-09-10.md` section 9 |
| Clock arms in the engine respond TOO STRONGLY to the pregame tempo prior (Decision 8 band) | **NEW 2026-09-11, and it separates the families** | Possessions by pregame tempo-prior quintile, 25 seeds, 100 games per bucket: every arm is monotone 4 of 4, so nothing is flat, but the slope ratio is 1.387-1.400 for the gamma family and 1.434 for the incumbent -- OUTSIDE Decision 8's [0.8, 1.2] -- while the two cell-based arms sit at 1.061 and 1.079, inside it. The offline round-1 read put all five arms in 0.79-1.06 because `chain_halves` replays the real sequence of previous-end types; only the engine, which simulates that sequence too, exposes the over-response. A second axis, independent of G1, on which the cell-based family is the better object. `clock/experiments.md` section 13.5 |
| The `score_diff` leak was also costing the engine 1.61 pp of eFG% | MEASURED 2026-09-10, resolved by the round-2 winner | On the same paired runs, using the FGM-by-class instrumentation added to the results contract the same day: engine eFG% **48.633% under S-A** against a realised 50.239%, and **50.061% under S-C** (-0.18 pp). Per class the gap closes exactly where the evidence doc said the leak was largest -- rim make% 55.777 (S-A) / 57.322 (S-C) / 58.475 (actual); three 32.659 / 33.734 / 33.483. Mechanism: fed a PRE-shot margin in the sim, a model trained on a POST-shot one under-predicts makes everywhere except in the blowouts it drove itself into. Not comparable like-for-like to the 60-game smoke read of 0.474 vs 0.509 taken under the reference event/clock arms. `fg_make/experiments.md` section 14.6 |
| G4 (implied team eFG%) under the round-2 winner | **REGRESSED on the DEFENCE tercile 2026-09-10 -- OPEN** | Worst as-of defence tercile gap: round-1 trio 0.914 pp PASS, S-C 1.066 pp FAIL against a 1.0 pp tolerance, and S-B / S-D / S-E all miss it too (1.021 / 1.078 / 1.061). The offence side passes for every arm (0.867-0.936). G4 is not a discard criterion in section 13.6 and did not change the decision; it is recorded as the round's one regression rather than buried. A 0.07 pp defence-tercile miss on a provisional truth table is not a reason to keep a post-outcome feature. `fg_make/experiments.md` section 14.7 |
| L3 FG-MAKE **ROUND 2b**: the round-2 winner S-C under the standing S1 scheme (L21, monthly in-season walk-forward refit) vs the same arm fitted once | **S1 ADOPTED on all three classes 2026-09-10; engine default UNCHANGED pending the PM's switch** | Pre-registered as `fg_make/experiments.md` section 15 AFTER round 2 decided and BEFORE 2b ran, because section 13 had already fixed the scheme at static so the five state arms would differ only in the state block. Six refits per class (the calendar months of the 2025 test season), the schedule taken from `possession_outcome.month_boundaries` rather than reimplemented. F2 log loss: rim 0.660471 -> 0.660150 (-0.60 floors), jumper 0.664094 -> 0.663985 (-0.19), three 0.591958 -> 0.591644 (-0.37) -- better on all three and on none by a full floor, **reproducing L21's 'S1 is a calibration fix, not a log-loss fix' on a second sub-model**. No gate moves in either direction: calibration PASS under both schemes on all three classes (three improves 0.852 -> 0.578 pp, jumper worsens 1.212 -> 1.693 pp, both inside the 2.0 pp gate), Decision 8 PASS throughout, and the FIRST-CHANCE segment gap improves on every class (0.145 -> 0.008, 0.368 -> 0.049, 0.233 -> 0.130 pp). Closed-loop gate re-run for the S1 arm: PASS on all four checks (margin SD 14.002, corr +0.229, poss 70.758, total bias +3.912) and engine eFG% 50.147% against a realised 50.239% -- the closest of any arm in either round. Cost: 18 artifacts instead of 3 and ~6x the fit time. Unlike possession_outcome there was no calibration FAILURE to repair here, so the honest summary is neutral-to-slightly-positive, and S1 is adopted on the standing-scheme rule rather than on a win. `fg_make/experiments.md` sections 15-16 |
| L4 usage ROUND 2b: S1 (in-season monthly walk-forward refit) vs the static fit, for the round-2 winner on each class | **S1 ADOPTED ON ALL FIVE CLASSES 2026-09-10; awaiting PM adoption of the artifacts** | Fold 2 (train 2024, test 2025), `lgbm` on all five classes (no runner-up inside the round-2 floor anywhere), S0 vs S1 paired, only the scheme moving. S1 improves log loss on every class -- FGA_rim -0.44, FGA_jump2 -0.54, FGA_3 -0.69, TOV -0.82, FT_trip -0.80 floors -- and improves the worst decile calibration gap on four of five (FGA_jump2 0.335->0.195 pp, FGA_3 0.318->0.146, FGA_rim 0.541->0.513, FT_trip 0.395->0.376; TOV worsens 0.236->0.375 but both schemes clear the 2.0 pp gate by 5x). Decision-8 slope ratio stays inside [0.8, 1.2] under both and moves CLOSER to 1.000 on three classes. No gate lost anywhere, so the pre-registered rule adopts S1. **NOT a calibration rescue, unlike L21**: the static allocator was already at 0.24-0.54 pp, so S1 is adopted because it is the standing default and costs nothing here, not because this model needed it -- a flat result is the expected result when the static fit already passes. Seed SD 2.0e-05..1.5e-04 vs bootstrap SE 0.000808..0.001696, so the bootstrap floor binds; S1's own bootstrap SE is 0.4-4.5% LARGER than S0's (refitting does not reduce sampling variance). Cost: 6 fits per class per season and a monthly deployment refit. 30 monthly artifacts + 5 static controls in `data/processed/models/usage_s1/` with a manifest the engine selects by game month. `docs/models/usage/experiments.md` section 9 |

## B. Features & training specs

| change | status | evidence / waiting on |
|---|---|---|
| Own ridge as-of ratings (off/def per 100 poss + tempo, prior-season shrinkage prior) | SHIPPED 2026-09-10 | `data/processed/ratings/own_ratings_{season}.parquet`; fitted lambda 5.0 / prior weight 0.8 for both models, selected on fold-1 training seasons only; walk-forward RMSE 13.706 (eff) and 4.732 (tempo). `experiments.md` R1 |
| Own ratings pass the INV-45 change-form leak test | VALIDATED 2026-09-10 | as-joined corr +0.054 / -0.062 / +0.002 vs a 0.15 gate and a 0.04-0.08 honest baseline; `data/processed/ratings/own_ratings_leak_test.csv` |
| Anchor feature set for the Control: A_own vs B_kp vs C_both | DECIDED: A_own 2026-09-10 | F2 margin MAE 9.146 / 9.075 / 9.059 against a measured seed noise floor of 0.047-0.077; the pre-registered tie clause falls through to A_own. Paired (MC-cancelled) SEs show B_kp and C_both hold small real edges (t = 2.4, 3.5) that the unpaired floor cannot resolve. `experiments.md` R4-R6 |
| Rate dispersion family per target (deviance/df > 1.2 rule) | DECIDED 2026-09-10 | NegBin for 3PA (dev/df 1.60, alpha 0.0268), 2PA (1.27, 0.0070), FTA (2.59, 0.0832); Poisson for TOV (1.06). Same decision on both folds. `experiments.md` R2-R3 |
| Site feature = home / away / neutral only | CONFIRMED-DEFECT (neutral bucket) 2026-09-10 | G6 neutral sim +2.23 vs actual +3.29 (-1.06, tol +/-1.0) while non-neutral passes at +0.33. Needs a designated-home or venue-distance term. Report section 10, D4 |
| L3 team-form features: as-of expanding 3PA/rim-share/TOV%/FTr, offence and defence-allowed, centred on the league's own as-of mean | SHIPPED 2026-09-10; SOURCE CHANGED 2026-09-10 (round 2) | `cbb_sim.models.possession_outcome.build_team_form`; leak-safety proved two ways in `tests/test_possession_outcome.py` (independent recomputation of the strictly-earlier average, and invariance to corrupting the game's own rows) |
| L3 style rates were built over the POSSESSION table, so continuation-chance labels reached a FIRST-chance model's own predictors | CONFIRMED-DEFECT, FIXED BY CONSTRUCTION 2026-09-10 | `off_rim_c` was `sum(fga_rim)/sum(fga)` over `possessions_{season}.parquet`, whose attempt columns are summed across every chance of the possession. 2025's continuation chances carried the ESPN putback mislabel below, so a team's own as-of `off_rim_c` was mechanically understated in 2025 and that understated PREDICTOR was fed to the `first` population -- whose targets the defect cannot touch. Matches the asymmetric round-1 miss exactly (FGA_rim -0.42 pp, FGA_jump2 +1.80 pp, FGA_3 -1.15 pp and untouched by this channel). Fixed by rebuilding the rates from FIRST chances only (`STYLE_SOURCES`, `team_game_box_first_chance`), which cannot read a continuation chance at all. `tests/test_possession_outcome.py::test_a_continuation_chance_label_cannot_move_a_first_chance_feature` relabels every continuation chance and requires the feature to be bit-identical, while showing the old source moves. `docs/models/possession_outcome/features.md` section 1.1 |
| L3 style rates from hoopR `team_box` instead of from events (the pre-registration's other option) | REJECTED 2026-09-10, with the reason measured | `team_box` carries no rim/jumper split at all, so `off_rim_c` -- the one rate the defect actually moved -- cannot come from it; and `team_box` is whole-game, so it carries the same continuation-chance contamination from a different feed. `scripts/diag_style_rate_sources.py` shows the first-chance event rate tracks the independent box-score rate as closely as the all-chances rate does for the three stats `team_box` can express. `features.md` section 1.1 |
| `is_transition` as currently defined is contemporaneous with the outcome it predicts | CONFIRMED-DEFECT 2026-09-10 | It is derived from the chance's own `duration_s`, so it is not available at the chance's start. Must be replaced by a pre-chance definition (previous possession's terminal event + seconds since the change of possession) before the model is wired into the engine. `docs/models/possession_outcome/model.md` section 9 |
| L5 feature blocks: A_state -> B_plus_teams (tempo priors + ratings + site) -> C_plus_score (score diff x clock + bonus) -> D_plus_season | B KEPT, C KEPT, D REJECTED 2026-09-10 | Against a 0.006764 floor: the team block gains 0.018-0.040 F2 CRPS in every arm (2.7-5.9x the floor) and the score/bonus block a further 0.019-0.068 (2.8-10x). The season block moves CRPS by -0.0037..+0.0020, entirely inside the floor, and makes the emergent mean WORSE on four of five arms (e.g. `gamma` +0.720 -> +1.346). A linear day counter is not the season-progress structure L5 asks for. `docs/models/clock/experiments.md` section 2.2 |
| `chance number within possession` as a pre-registered L5 A_state feature | DEGENERATE AT A POSSESSION'S START 2026-09-10 | A possession begins on its first chance by the segmentation rule, so the column is identically 1.0 in all 2,607,192 rows. It is KEPT in the pre-registered list, reported as zero-variance, and dropped by every arm with the drop recorded -- never silently ignored. If a CHANCE-level duration target was intended (L3 is chance-level), that is a different target needing its own pre-registration. `clock/model.md` sections 6 and 9 item 6 |
| `is_transition` banned outright as an L5 feature (not merely flagged as at L3) | SHIPPED 2026-09-10 | It is defined as `duration_s <= 8 AND start_reason in {DREB, TOV}` -- a function of the L5 TARGET, so a perfect leak here rather than the contemporaneity problem it is at L3. `cbb_sim.models.clock.BANNED_FEATURES` plus three tests: the identity is proved on the real 2024 table, the loader is shown never to read the column, and permuting `terminal_event` within every game leaves every feature column bit-identical while the censoring flag moves |
| CBBD-completeness game filter (possession points + technical points must equal the schedule final for BOTH teams) | SHIPPED (built at L5) 2026-09-10 | `cbb_sim.models.clock.cbbd_complete_games`. Removes 3,067 of 21,969 games (14.0%) over 2022-2025, concentrated in 2022-2023 exactly as `docs/tests/possessions_build_2026-09-10.md` section 2 predicts. This is the first implementation of the CBBD-side flag the section-D data-defect row has been waiting on; it should be pushed back into `games_universe` so every model uses one definition. A second, CLOCK-accounting filter was considered and REJECTED: the signed deviation of a half's summed durations from 1200 s is only -0.75 s, while a +/-2 s filter would remove two thirds of the universe |
| `unknown`-terminal possessions KEPT at L5 (they are dropped at L3) | DECIDED 2026-09-10 | L3 drops them because the terminal LABEL is its target and the label is a data gap; L5's target is elapsed clock, which is observed regardless, and dropping them would punch holes in the tiling of the period the emergent test walks. Cost measured, not avoided: on F2 their actual mean duration is 6.48 s against a model-implied 19.31 s (6,005 rows, 0.8%), which biases the emergent count DOWN by ~0.4 possessions per team -- so the reported overshoot is if anything understated. `clock/model.md` section 4.5 |
| L3 rebound team form: as-of OREB% and DREB%-allowed built from FIRST-CHANCE opportunities only, league-centred | SHIPPED 2026-09-10 | `cbb_sim.models.rebound.team_rebound_form`. First-chance-only is the pre-registration's 'first-chance-safe sources' clause and closes the same contamination channel the L3 style rates hit (ledger row B5): pooling continuation chances would make 'as-of OREB%' partly a function of how many second chances a team happened to get. Leak safety proved two ways in `tests/test_rebound.py` (independent recomputation of the strictly-earlier average, and invariance to corrupting the game's own rows while a LATER game does move) |
| FT-2 shooter identity keyed on the CBBD player id rather than the ESPN athlete id | DECIDED 2026-09-10, against the pre-registration's stated preference, with the reason measured | The pre-registration prefers the ESPN id 'via the crosswalk if present'. It is present on 100.0% of 2024 attempts and 100.0% of 2025 but 0.0% of 2022 and 2023 -- CBBD rosters were pulled for 2024-2026 only -- so it cannot span the fit window (52.67% overall). The CBBD id can, and it maps to the SAME ESPN athlete id on 100.0% of the 9,421 players the crosswalk covers in both 2024 and 2025 and the 3,730 covered in both 2025 and 2026, so it is a stable cross-season identity rather than a per-season surrogate. The ESPN id is still attached wherever it resolves, for the L4 join. `docs/models/free_throw/model.md` section 6 |
| Empirical-Bayes prior for a shooter's free-throw rate: league mean vs POSITION mean vs the player's prior-season rate | DECIDED: position mean, strength m = 30 pseudo-attempts 2026-09-10 | Both the prior and the strength were fitted on the training fold and BOTH folds independently chose `position` at m = 30. A shooter's own rate carries half the weight at 30 attempts on the season, three quarters at 90 and nine tenths at 270; the median F2 attempt is taken by a shooter with 32 prior attempts, and 51.04% of F2 attempts are past the crossover. The position prior beating the prior-season prior is the same transfer-attenuation shape L15 measured, arrived at independently. Transfer subset: `eb_shrink` log loss 0.57951 on players whose modal team changed vs 0.569852 on continuing players and 0.595113 on players with no prior season at all. `docs/models/free_throw/experiments.md` section 5 |
| Technical free throws in the FT-2 universe | EXCLUDED 2026-09-10, with the reason measured | 8,254 of 809,294 attempts, make rate 0.79561 against 0.71804 for everything else. The shooter on a technical is chosen by the coach rather than by who was fouled, so the two populations are drawn from different shooter distributions and pooling them would bias both. The engine needs its own rule for them: `docs/models/free_throw/model.md` section 9 |
| L4 usage inputs: each player's as-of per-possession-ON-FLOOR rate by event class, shrunk toward a fitted prior with a fitted strength | SHIPPED 2026-09-10 | `cbb_sim.models.usage.build_player_asof` / `shrunk_rate`. The denominator is the number of credited events of any of the five classes that happened while the player was one of the OFFENSIVE five, strictly earlier in the season -- a per-possession on-floor exposure, not a per-game one, which is what makes a bench player's rate comparable to a starter's and what lets the fitted `m` read directly as "pseudo on-floor events of history". Leak safety proved two ways in `tests/test_usage.py` (an independent strictly-earlier recomputation of every numerator and denominator, and invariance to corrupting the row's own game while a LATER game does move). hoopR minutes join through the player crosswalk on 99.81% and roster position resolves on 99.98% |
| L4 usage roles (primary handlers / wings / bigs) from position group + as-of usage rank | SHIPPED 2026-09-10 (used by the hierarchical arm only, which was not adopted) | `cbb_sim.models.usage.assign_roles`: a centre is a big; among the non-centres the top `HANDLER_RANK` by shrunk as-of TOTAL usage rate are handlers; the rest are wings. The rank is over the lineup, which is the candidate set the sampler is handed; the rate is entirely pregame and is built by the same `shrunk_rate` code as a real class so the role cannot be assigned off a differently-constructed rate. Per-role "too narrow" ratios 0.948-1.175 |
| The prior-season usage block is UNIDENTIFIED on fold F1 and is dropped with the drop recorded | SHIPPED 2026-09-10, with the cost of not doing it measured | On-floor ids start in 2024 (L13), so a 2024 row has no prior-season on-floor history and a 2025 row does: the block is identically absent in F1 training and present in F1 test. Worse than a constant column, because `build_usage_design` falls `prev_rate_{class}` back to the POSITION prior, so the column still VARIES and a constant-column check does not catch it -- left in, the F1 conditional logit on FGA_3 scored 3.74 against U1's 1.46. `usage.unidentified_features` returns `log_prior_share` and `prior_rate` on any fold with no prior-season history, the trainer drops them, and the drop is printed in every per-class block. Same treatment `cbb_sim.models.clock` gives its degenerate chance-number column. The within-2025 fold has the block active on both sides and the shrinkage grid still never chooses the prior-season rung there |
| State features in the L4 conditional logit enter only as interactions | SHIPPED 2026-09-10, as an identification fact rather than a preference | A conditional logit differences out anything constant across the alternatives, so `score_diff`, `sec_remaining` and `chance_number` as plain columns have no estimable coefficient at all. They are pre-registered, so they are kept and interacted with the alternative's own log share and with `is_C`. The raw columns go to the tree arm unchanged, where a tree can interact them itself. `docs/models/usage/features.md` section 3 |
| L4 `chance_number` is a PROXY (offensive rebounds since the last possession-ending event), not the possession machine's value | FLAGGED 2026-09-10 | The authoritative `chance_number` lives on `chances_{season}.parquet`, which carries no on-floor five to join an event to, and the possession table carries the five only once per possession. Fixing it means emitting a per-event chance index from the possession builder -- another worker's module, not changed here. `docs/models/usage/features.md` section 1.3, `model.md` section 9 item 5 |
| L3 FG-make team form: as-of make rate per shot class, offence and defence-allowed, centred on the league's own as-of rate | SHIPPED 2026-09-10 | `cbb_sim.models.fg_make.team_shot_form`. Built from ALL chances rather than first chances only (the rebound model's clause above) because this model's target population is all attempts and the L16 contamination channel is repaired at the SOURCE by the v2 rim override. Cost measured, not assumed: rebuilding every team rate from first chances alone moves F2 ridge log loss by -0.00001..+0.00002 (0.02 floors) in all three classes, the two sources correlating 0.949-0.958. `fg_make/experiments.md` section 7 |
| `minutes-to-date`, a pre-registered L3 FG-make feature | COULD NOT BE BUILT 2026-09-10, substituted with the reason measured | Minutes live in hoopR `player_box` behind the ESPN athlete id; the CBBD->ESPN crosswalk resolves 100% of 2024 and 2025 attempts and 0% of 2022 and 2023 (rosters were pulled for 2024-2026 only), and CBBD `onFloor` cannot substitute because it is empty at the source before 2024 (L13). A column that is real in two training seasons and structurally zero in the other two is a season dummy wearing a minutes label. `shooter_games_asof` + `shooter_fga_asof` carry the exposure instead. `docs/models/fg_make/features.md` section 3 |
| The assisted flag as an L3 FG-make feature | EXCLUDED BY THE PRE-REGISTRATION, NOT BUILT AT ALL 2026-09-10 | In this feed an assist is logged as a property of a MADE basket, so the flag is knowable only after the ball goes in. Together with `blocked` (a Block Shot row exists only because the attempt missed) and `and_one` (the foul is logged after the make) it is named in `fg_make.BANNED_FEATURES`, and `design_matrix` raises on all three so a later reader cannot add one by accident. `tests/test_fg_make.py` |
| The chance-state block (`chance_number`, `chance_elapsed_s`, `is_transition`) at L3 FG-make | KEPT 2026-09-10, and distinguished from the banned L5 `is_transition` | The banned definition is a function of the chance's own `duration_s`, i.e. of when the chance ENDS (the L5 target, and contemporaneous with the L3 terminal event). This one is a function of when the chance STARTED plus the attempt's own clock, which is known at release because the attempt's existence and timing are GIVEN and only make/miss is modelled. Proved rather than argued: flipping an attempt's own `made` flag leaves its own chance number, elapsed, start reason and transition flag bit-identical, and the derived chance offence matches the shooter's own team on 99.32% of attempts. Worth 19.0 / 9.9 / 13.2 floors; on `FGA_rim` it is what fixes a 3.25 pp putback (continuation-chance) miss. `fg_make/features.md` section 4 |
| Centred as-of rates written as a SUBTRACTION rather than a branch | CONFIRMED-DEFECT, FIXED BY CONSTRUCTION 2026-09-10 | `raw - league` in float32 leaves a ~1e-8 residue in a column that is structurally CONSTANT on a fold (a prior-season rate in the first season of the data). A standardiser's absolute `sd < 1e-8` guard does not catch it, divides by the residue, and turns a zero-information column into a 1e7-sized input: the linear arms diverged to a log loss of 10.0 on the first smoke run. Fixed at both ends -- every centred rate is now `where(the rate exists, rate - league, 0.0)` (the "no prior attempts means exactly the league mean" convention), and `RidgeArm`'s constant-column threshold is RELATIVE to the column's own level. `fg_make/features.md` section 4 |
| Empirical-Bayes prior for a shooter's make rate by shot class: league / POSITION / prior-season, with the defence entering multiplicatively on the logit scale | DECIDED: position mean, strengths fitted per class 2026-09-10 | The position prior wins in all three classes on BOTH folds -- the same answer the free-throw model reached (L18) and the shape L15 predicted for transfers. Fitted shooter strengths: 30 (rim), 75 (jumper), **5** (three) pseudo-attempts, the smallest shrinkage anywhere in the cascade so far: a shooter's own three-point rate is trusted after five attempts and 88.6% of F2 three-point attempts are past the crossover. The DEFENCE's strength is fitted too (300 / 300 / 1000), because its as-of allowed rate rests on a handful of attempts in November and thousands in March. Transfer subset: the EB arm's predicted make rate tracks the realised rate within 0.6 pp on transfers in every class. `fg_make/experiments.md` section 5 |
| LightGBM parameter search for L3 FG-make, on F1 ONLY | SHIPPED 2026-09-10 | Six rungs scored on fold F1's test season, frozen per class before F2 is touched and reused for the seed refits and the lineup fold. All three classes independently chose the same rung (31 leaves, `min_child_samples` 200). Cached to `data/processed/models/fg_make/lgbm_ladder_v2.json` so a re-run cannot re-tune. `fg_make/experiments.md` section 4 |
| CBBD <-> ESPN **player** id crosswalk (the `SIM_GUARDRAILS` section 4 prerequisite for every lineup feature) | SHIPPED 2026-09-10 | `src/cbb_sim/data/player_ids.py`, `scripts/build_player_crosswalk.py`, `data/processed/player_crosswalk.parquet`. **Possession-weighted match rate 99.9868% overall (100.0000% in 2024, 99.9991% in 2025, 99.9618% in 2026)**; 99.86% of the 14,986 players who ever take the floor. Built in **3 API calls** -- CBBD `/teams/roster?season=S` returns every team's roster with the ESPN athlete id as `sourceId`, so 33,375 of 34,938 rows are exact-and-verified and name/jersey matching did no real work. End-to-end validation: CBBD-derived minutes vs hoopR box minutes corr 0.9841, mean diff +0.14 min, MAD 0.63 min; hoopR `did_not_play` agrees with zero on-floor minutes on 99.02% of as-of rows. `rotation/model.md` section 4 |
| L4 as-of player features (shift(1) expanding minutes/starter/DNP/foul history, role-prior shrinkage, rotation depth) | SHIPPED 2026-09-10 | `cbb_sim.models.rotation.build_asof_player_features`; leak-safety proved two ways in `tests/test_rotation.py` (independent recomputation of the strictly-earlier average, and invariance of every as-of column to corrupting the game's own rows), and the contemporaneous columns the expanding sums were built from are dropped from the returned frame so a consumer cannot reach them |
| Recency-weighted starter predictor (EWMA of the starter flag) vs a flat expanding starter frequency | DECIDED: EWMA alpha 0.50 2026-09-10 | overlap with the real starting five 4.60 of 5 vs 4.22 for the flat mean, fitted on 2024. Matters because the state-dependence metric counts the five the model started: the residual 0.43-player error costs 1.6-2.8 pp of the late-window starters' share on its own, measured by re-grading the ACTUAL on-floor sequence with the model's starter set. `experiments.md` section 2.4 |
| Availability as an i.i.d. per-game Bernoulli draw from a fitted P(plays \| as-of rank) table | KEPT BUT CONFIRMED-DEFECT 2026-09-10 | It is the only mechanism that produces the exact zeros a real minutes table is full of -- without it the simulated nonzero-minute count is 12.1 players against an actual 9.6 -- but drawing it independently each night is the diagnosed cause of the **within-player minutes SD ratio 1.30-1.35** (pooled SD ratio is fine at 1.02-1.06). A real player is unavailable in runs, not independently. Fix is a Markov availability state; not built |
| Foul-trouble tilt estimated by stratification | REFUTED 2026-09-10 | Pooled, the data says three fouls makes a player **3x MORE likely** to be on the floor; controlling for as-of rank it still says 1.6x; controlling for rank AND minutes-played-so-far 1.4x -- a foul count is itself a record of floor time. Replaced by a within-player event study (on-floor rate in the 20 possessions after picking up foul f over the 20 before, ratios chained), which gives 0.84 / 0.39 / 0.17 / 0.17 cumulative in the first half. `cbb_sim.models.rotation.foul_tilt_from_event_study` |
| Scheduler urgency = remaining deficit against a game TOTAL, or against a cumulative PATH | REFUTED 2026-09-10 | The total form equalises every player toward his target by the end of the game; the path form is better but sluggish -- starters took 0.65 of the final eight minutes against an actual 0.78, below even their 0.70 game average. Shipped form tracks the state-tilted minutes RATE with an exponentially-weighted on-floor tracker plus a slow long-run correction, and still hits the drawn Dirichlet target in expectation |
| Threshold-free lineup re-evaluation at every state-cell change; and ranking a period-boundary restart by path deficit | REFUTED 2026-09-10 | Both put every resting player ahead of every on-floor one, because the urgency signal is "how far below your rate are you right now". The state-change version cost 3-4 pp of the starters' share in the final eight minutes. A period boundary now re-sets the floor to the top five by target RATE |
| R5's override as a Markov bench/return flag, and its strength fitted against the substitution rate | REFUTED 2026-09-10 | The fitted base rates imply an equilibrium blocked fraction of 0.044/(0.044+0.022) = 2/3 even in an ordinary state, so it churned the lineup and the fit went to the grid ceiling (scale 16) with the substitution rate at 0.212 vs a real 0.152. Fitted against the substitution rate it is selected out entirely (scale 0.00 = plain R4), because the donor already substitutes slightly more often than reality. Respecified as a state-DEVIATION block with one per-game coach-tolerance draw, strength fitted against the four state cells on train |
| L5 clock ROUND 3: horn censoring (`end_clock <= 0`), KM tail rule, truncated-CRPS metric, clock-complete gate universe, XGBoost survival:aft x 9 arms incl. 3 old-flag paired controls | NO ARM ADOPTED 2026-09-10 | 0 of 9 pass the three pre-registered gates on F2 (emergent G1 on clock-complete games, PIT on the truncated law, end-of-half on clock-complete halves). Floor 0.006376. Best G1 in three rounds: `empirical_km3_srfloor` +0.886, the first arm ever inside the +/-1.0 mean tolerance, failing on SD (-0.884), months (1/5) and EOH duration (-1.46 s vs 0.254 s). The fix is real (see the L20 row) and not big enough. `clock/experiments.md` sections 8-9 |
| Censoring-aware TREE loss (XGBoost `survival:aft`, error distribution chosen on F1 from normal/logistic/extreme) as the L5 arm | REFUTED 2026-09-10 | F1 chooses `extreme` (Weibull), censored log-likelihood -3.576 vs -3.669 / -3.758, scale 0.5293 by profile likelihood. On F2: CRPS_trunc 4.9484 (7th of 9), G1-CC +1.212, EOH -2.24 s, 37 leak-sized PIT cells. Better than the round-2 quantile reference on every GATE and worse on CRPS, so the tree class is not where the remaining error is. `clock/experiments.md` section 9.3 |
| Nine-quantile LightGBM as the L5 sampler, re-tested under the censored metric | REFUTED AGAIN, now on a proper score 2026-09-10 | Lowest CRPS_trunc in the round-3 grid (4.8386) and the WORST censored log-likelihood (-5.117 vs -3.516 for `empirical_km3`): 206 test rows get zero predictive mass below their own remaining clock and censored rows get almost none above it. G1-CC +2.227 and EOH -6.31 s, both worst in the grid. Round 1 called this a truncated tail from the CRPS side; the censored likelihood now prices it. `clock/experiments.md` section 9.3 |
| Truncation-aware scoring of a censored duration target (renormalise the predictive pmf onto {0..R-1} before CRPS/PIT on uncensored rows; censored log-likelihood as the only metric censored rows enter) | SHIPPED 2026-09-10 | Not cosmetic: worst powered PIT K-S D is 0.095-0.235 on the corrected arms against 0.52-0.56 on every old-flag arm, a mis-specification round 2's untruncated metric could not see. `cbb_sim.models.clock_v3.truncate_pmf` / `censored_loglik_rows`; `clock/experiments.md` section 8.4 |
| CBBD clock-completeness flag in the games universe, and G1/end-of-half re-based on clock-complete games | SHIPPED 2026-09-10 | Three-part definition (last possession ends within 2 s of the horn, first starts within 2 s of the period length, summed durations within 4 s of it). 34.5-37.4% of games by season. Published as a VERSIONED SIBLING `data/processed/games_universe_v2.parquet` (+ `clock_flags_evaluated`, since sealed 2026 is never evaluated) plus half- and possession-level side tables; `games_universe.parquet` untouched. Worth 0.00 to -0.20 possessions per team-game, i.e. it makes the round-2 arms look slightly WORSE -- a grading-truth fix, not a gap-closing one. Closes the round-2 open row. `docs/tests/clock_censoring_audit_2026-09-10.md` |
| L5 binned lookup-table export (engine throughput deliverable, `engine/RESUME.md` section 3 item 4) | MEASURED 2026-09-10 -- ERROR IS NOT SMALL | On the best-CRPS arm over the 1,080-cell x 91-duration grid (187 cells empty): dCRPS_trunc +0.0687 (10.8x the 0.006376 floor), TV distance mean 0.1032 / max 0.9252, emergent G1-CC mean delta +0.459 possessions WORSE (live +2.227 -> binned +2.686). Throughput 13,728 -> 183,550 rows/s, 13.4x. The deliverable warned against assuming the error was small; it is not, on a tree arm. A cell-based arm IS a lookup table on these dimensions and has structural zero binning error wherever a cell is populated, so that is the cheap path if one ever wins. `data/processed/models/clock/v3_lookup_report.json` |
| L5 round 3b: censoring x state parametrisation (as designed / `score_diff` removed / engine-safe end-game indicators) with the Decision-10 closed-loop paired-stream gate, then the S1 scheme confirmation | OFFLINE STAGE RUN, NO ARM ADOPTED 2026-09-10; CLOSED-LOOP GATE PENDING | 0 of 6 pass the offline gates. Best G1-CC the project has produced: `gamma_aft|P3` +0.903 mean / -0.277 SD, the first arm with BOTH inside tolerance, failing on months (2/5), end-of-half duration (-1.51 s vs 0.254 s) and PIT (30/30). Cumulative overshoot +1.516 (round 2) -> +0.885 (P3 + S1), 58% removed by two pre-registered changes and no tuning. `clock/experiments.md` sections 10-11 |
| Deleting `score_diff` from the clock model (P2) as the fix for the emergent possession overshoot | CANNOT BE DECIDED OFFLINE 2026-09-10 -- closed-loop evidence required | P2 lands BETWEEN P1 and P3 on both base arms (empirical +1.276 vs +1.357 / +1.131; gamma +1.092 vs +1.130 / +0.903), nowhere near the engine's 71.6 -> 67.84. The reason is structural: `clock.chain_halves` feeds the model the REAL score sequence and overrides only clock-derived columns, so there is NO feedback loop offline for P2 to break. The offline grid measures the INFORMATION in `score_diff` (0.08-0.23 possessions) and is incapable of measuring the LOOP (~3.8 possessions in the engine). Exactly the failure mode Decision 10 exists for. `clock/experiments.md` section 11.2 |
| Engine-safe end-game state for L5 (margin as three coarse indicators -- intentional-foul / run-out-the-clock / close -- active only in the last 120 s of H2 or OT, `score_diff` otherwise absent) | BEST OFFLINE G1 SO FAR 2026-09-10, NOT ADOPTED | Improves the emergent count on BOTH base arms against both P1 and P2 (empirical +1.357 -> +1.131; gamma +1.130 -> +0.903) at a CRPS cost of 0.009-0.020, i.e. 1.5-3 floors. Still fails months, end-of-half duration and PIT. Adoption waits on the Decision-10 closed-loop gate. `clock/experiments.md` section 11.1 |
| S1 (in-season monthly walk-forward refit) as the L5 training scheme | ADOPTED AS THE SCHEME 2026-09-10 (the arm is still not adopted) | On `gamma_aft|P3`, six monthly refits: CRPS_trunc -0.000754 (0.12 of the floor, a wash, exactly as L21 predicts), and NO pre-registered gate regresses -- G1-CC +0.903 -> +0.885, SD -0.277 -> -0.269, EOH duration -1.509 -> -1.476, PIT unchanged at 30. Per-month artifacts + `manifest.json` persisted under `data/processed/models/clock/v3b_s1/` for engine selection by game date. `clock/experiments.md` section 11.4 |
| Decision-10 closed-loop paired-stream gate on the L5 clock arm | PENDING 2026-09-10 -- BLOCKS ADOPTION | `scripts/diag_engine_multilevel.py` grades an existing results directory; it does not run the engine with a swapped clock model, so the gate needs an engine run with `ENGINE_CLOCK` pointed at `v3b_arm_gamma_aft_P3.pkl` (or the S1 manifest) -- the engine worker's harness and adapter. Left as a handoff rather than forced from another worker's lane. Nothing in round 3b is adopted until it runs, and section 11.2 shows the offline stage cannot decide the question without it |
| L5 round-2 per-game count SD | INSIDE TOLERANCE on three arms 2026-09-10 | `gamma` -0.194, `hazard` -0.248, `lognormal` +0.259 against the +/-0.75 G1 tolerance (round 1's best non-lognormal was -0.256). Dispersion is no longer the blocking half of G1; the mean and the end-of-half shape are. `clock/experiments.md` section 6.2 |
| L5 round-2 state columns are recomputed from the SIMULATED clock inside the emergent chain | SHIPPED 2026-09-10 | `cbb_sim.models.clock.apply_clock_override` is the only place that knows the clock-derived columns, and `tests/test_clock.py::test_chained_sim_does_not_inherit_the_real_clock_bucket` proves it by corrupting every round-2 clock column in the design to a constant and requiring bit-identical chained possession counts. Without this the emergent test would read the real clock bucket -- the answer |
| L4 attribution inputs: each player's as-of rate for the credit, denominated in OPPORTUNITIES OF THAT TARGET'S OWN POPULATION while on the floor | SHIPPED 2026-09-10 | `cbb_sim.models.attribution.build_player_asof` / `shrunk_rate`. A player's `REB_def` exposure is the live defensive rebounds his team won while he was one of the defensive five, so the candidates' rates sum to about one over the candidate set and the fitted `m` reads as "pseudo opportunities of history". This is a DIFFERENT denominator from `rebound.player_rebound_rates` (every miss while on the floor, i.e. a share of AVAILABLE rebounds); both are documented and the two numbers are not interchangeable. Leak safety proved two ways in `tests/test_attribution.py` (an independent strictly-earlier recomputation, and invariance to corrupting the row's own game and a LATER game while an EARLIER game does move it). `docs/models/attribution/features.md` section 2 |
| L4 attribution arms see ONLY the pre-registered features -- no exposure, no minutes, no lineup aggregate | SHIPPED 2026-09-10, as a contract decision with the cost acknowledged | The pre-registration's player input list is the as-of rate, the prior-season rate and the position group, so a player with two opportunities of history and one with two hundred are separated only through the shrinkage. That almost certainly leaves signal on the table; adding a column would be an unregistered feature bundle, so it is recorded as a known gap needing a NEW pre-registration instead. `docs/models/attribution/model.md` section 9 items 3-4 |
| The L4 attribution BINARIES are gated on the per-TEAM-GAME count and its dispersion, not on usage's per-player trio | SHIPPED 2026-09-10, with the reason recorded rather than the check quietly dropped | A binary credits no player, so "per-player per-game count SD", "players with >= 1 credited" and "top-1 / top-3 share" have no player axis to live on. The same-family statistic one level up does exist and is what the binary is held to: the mean count per team-game (+/- 0.5) and its SD ratio (0.9-1.1), via `attribution.binary_game_level_check`, which reports the three per-player fields as `null` with that reason. The player axis returns in `attribution.composed_check` (binary then choice), which is a REPORTED DIAGNOSTIC and never enters `decide`. `docs/models/attribution/model.md` section 3.2 |
| Decision 8 is read in this model as "slope ratio in [0.8, 1.2] AND >= 3 of 4 monotone steps, sub-2-pp drivers exempt from BOTH clauses" | SHIPPED 2026-09-10, with both readings reported | This model's pre-registration cites Decision 8 directly instead of restating a step count, so the decision's own minimum (3) is the requirement. `usage` reads clause (b) as 4-of-4 relaxing to 3-of-4, because ITS pre-registration specified 4 of 4 and Decision 8 only relaxes it. `attribution.decision8_verdict` stores `strict_4of4_pass` next to `pass` on every arm, so the stricter reading stays auditable and the divergence between the two models is a documented reading rather than a silent one |
| L3 FG-make round-2 state block: `score_diff_pre` and the three saturating indicators `gt_flag` / `eg_trail` / `eg_lead` | BUILT AND MEASURED 2026-09-10; only `gt_flag`'s family is in a winning arm's REJECTED set -- the adopted S-C carries NONE of them | Thresholds were FIXED IN THE PRE-REGISTRATION from the step-1 evidence grids and never re-tuned: `gt_flag` = |margin| >= 15 and <= 8:00 left in regulation (step-1 effect +4.79 / +1.74 / +0.53 pp, and flat at +4.3..+5.2 pp on rim across all NINE cells of a 3x3 threshold grid); `eg_trail` = trailing 1-9 inside 2:00 (-7.78 pp on threes, -10.57 inside 1:00); `eg_lead` = leading 1-9 inside 2:00 (+2.75 / -2.99 / -3.28 pp). All three fire on 0.5-6.4% of the F2 test season and `eg_trail`'s raw three-point make rate there is 26.74% against a 33.80% base -- the effect reproduces out of sample. They still lose: S-D is 0.64 / 0.04 / 0.09 floors from S-C, inside the floor, and the tie-break takes the simpler arm. The honest reading is that the end-game effect is real but is carried by the SHOT MIX (3PA share 52.0% trailing 4-9 inside 2:00 against 27.0% leading 4-9, at 8.4 s vs 19.0 s on the ball), which belongs to possession_outcome and the clock, not to fg_make. `docs/tests/fg_make_state_confound_2026-09-10.md` section 3, `fg_make/experiments.md` 14.2 |
| Garbage time as a LINEUP-quality effect carried by the on-floor five | **REFUTED 2026-09-10** | On the 1,202,616 attempts of 2024-2025 carrying all ten on-floor ids, adding the defensive five's as-of rim and three-point allowed rates (shrunk at the round-1 fitted 100 pseudo-attempts) moves the adjusted margin-bucket span by **-0.10 / +0.01 / -0.01 pp** on a 4.88 / 2.72 / 1.29 pp span, and moves the garbage-time indicator's marginal effect by at most 0.10 pp in all nine threshold cells. Independent of, and agreeing with, round 1 section 8's TEAM-LEVEL verdict. `docs/tests/fg_make_state_confound_2026-09-10.md` section 2 |

## C. Engine & config

| change | status | evidence / waiting on |
|---|---|---|
| Counter-based RNG keyed on (seed, game_id, family) | SHIPPED 2026-09-10 | `src/cbb_sim/control/rng.py`; `tests/test_control.py` re-simulates a 37-game subset and requires bit-identical draws, and checks chunk-independence |
| Feature preflight before any draw | SHIPPED 2026-09-10 | `cbb_sim.control.models.preflight` raises `FeaturePreflightError`; every persisted model carries its feature list and train-time medians |
| Artifact writer stamps created_at / tipoff / backtest | SHIPPED 2026-09-10 | `scripts/run_control.py`; `--live` asserts created_at < tipoff, backtests are stamped rather than faked |
| Decision rules that need to resolve < ~0.05 margin MAE must use the paired per-game SE, not the unpaired seed-offset floor | OPEN | `experiments.md` R5-R6; MC SE of a game's predicted margin is 1.39 points at 200 seeds |
| `pace.py` predictive sampler reuses `cbb_sim.control.rng` verbatim, keyed on (seed, game_id, "pace") | SHIPPED 2026-09-10 | `tests/test_pace.py` bit-identical/seed-independence checks, same contract as the Control |
| The NCAA bonus era as a GameState field rather than a model constant | SHIPPED (the artifact) / WAITING ON THE ENGINE 2026-09-10 | `data/processed/models/free_throw/bonus_era.json` carries the per-season (bonus_prior_fouls, double_bonus_prior_fouls) pair DERIVED from each season's own data, and `cbb_sim.models.free_throw.load_bonus_era()` reads it. No fitted object in that module carries a season-specific rule constant, per `CLAUDE.md` 'rule-era flags live in GameState, not baked into sub-models'. `tests/test_free_throw.py::test_the_thresholds_are_arguments_so_an_era_change_is_representable` requires the class boundaries to move when the thresholds do |
| Rebound and free-throw models read a VERSIONED possessions build through one flag | SHIPPED 2026-09-10 | `--version v1|v2` on both trainers. For the rebound model it moves exactly one feature -- the rim/jumper label of the miss (L16) -- and the threshold is read out of the chosen build's own `build_report.json` (`event_stream.rim_override_for_version`: 0.0 ft for the version this run used) rather than assumed. For the free-throw model it moves nothing, and `train_free_throw_v1.py --check-versions` ASSERTS it: the attempt table built under v1 and under v2 is identical over all 809,294 rows |
| `usage.py` sampler reuses `cbb_sim.control.rng` verbatim, keyed on (seed, game_id, "usage") | SHIPPED 2026-09-10 | `usage.new_game_state` draws the per-game usage realisation once off that stream by exact inverse-CDF gamma, and `usage.draw_player(five, event_class, state)` consumes one uniform per credited event. `tests/test_usage.py` requires the same (game, seed) to repeat, a different seed or game to differ, the counter to advance per call, and the empirical frequencies with no dispersion to equal U1's shares over the five |
| The usage allocation uniform was keyed on the GAME, so every event of a team-game drew the SAME uniform | CONFIRMED-DEFECT, FIXED 2026-09-10 | Measured before the fix: 3.42 players with a three-point attempt per team-game against a real 6.69, top-1 share 60.88% against 32.33%, SD ratio 2.46 -- the whole game collapsed onto one player. Fix: `usage.event_stream_keys` folds the event's ordinal within its game into the key (`game_id * EVENT_KEY_STRIDE + ordinal`), which keeps the per-game independence the RNG rule exists to give while making a game's own draws independent of each other. Pinned by `tests/test_usage.py::test_c_event_stream_keys_are_distinct_within_a_game` and `::test_c_consecutive_draws_are_independent_not_one_uniform_reused` |
| The L4 tree arm's objective: a binary LightGBM softmaxed within the five | CONFIRMED-DEFECT, FIXED 2026-09-10 | A per-row binary objective optimises a different likelihood than the grouped choice being graded, and came out systematically over-sharpened: on F1 TOV the top as-of-rate decile read 30.13% predicted against 27.89% real and the bottom 11.67% against 13.16%, a 2.24 pp worst gap that FAILED the calibration gate on an arm whose log loss (1.579167) looked fine. Replaced by `usage._group_softmax_objective` (`grad = p - y`, `hess = p (1 - p)` within each group of five), offset by `log q` (`usage.lgbm_init_score`) so the arm nests U1 exactly at zero trees: the same class now reads a 0.27 pp worst gap and 1.577664, and trains three times faster. This is an objective fix, NOT a temperature fitted on the model's own output -- the latter is the shape `SIM_GUARDRAILS` section 5 bans. Row bagging is also off in this arm, because a row is one alternative of a choice set |
| Two usage samplers, one distribution: exact inverse-CDF for the engine, batched numpy for the trainer | SHIPPED 2026-09-10 | `usage.realize_shares` is a pure function of (seed, game_id, draw index) and is called once per game, so its cost is irrelevant; `usage.realize_batch` runs the same generative form over every profile in one vectorised gamma call, which is what makes a 200-draw season-wide Monte Carlo affordable (26 million gamma variates). `tests/test_usage.py` asserts the two agree on mean and SD and pins the single-level form to the closed-form Dirichlet SD -- the same batched-twin discipline `cfb-props-sim/tests/test_usage_alloc.py` uses |
| A usage winner that is a tree or a logit still needs its sim-time lookup table | OPEN | `CLAUDE.md` forbids live model calls in the sim loop. `usage.draw_player` serves the rate-table arms (U1-U3) directly; the adopted tree needs its per-event scores materialised against the pregame feature bins before the engine can use it. `docs/models/usage/model.md` section 9 item 7 |
| `rotation.py` sampler reuses `cbb_sim.control.rng` keyed on (seed, game_id, "rotation"), home drawn before away | SHIPPED 2026-09-10 | `tests/test_rotation.py`: bit-identical re-simulation, a different seed moves the lineups, a game's draws are independent of which other games are in the run, and the engine-facing `RotationSampler.next_lineup(state)` reproduces the offline `simulate()` sequence exactly so the engine and the bake-off cannot be two models. The counter-based key is used verbatim; only its expansion into the few thousand sequential draws a rotation game needs is a PCG64 seeded from that key |
| Five-on-floor invariant | SHIPPED 2026-09-10 | asserted per possession for all six arms (exactly five slots, five distinct players, all from the team's own candidate list, nobody carrying five fouls), plus the degenerate case where the availability draw leaves almost nobody available. Two real bugs were caught by it: R3 could leave a fouled-out player on the floor when more exits were drawn than bench players available, and the arms disagreed on whether the recorded foul state was as-of the start or the end of the possession |
| `attribution.py` samplers run on FOUR RNG families, one per credit (`attr_rebound`, `attr_assist`, `attr_steal`, `attr_block`) | SHIPPED 2026-09-10 | `attribution.new_game_state` opens one stream per credit and `draw_rebounder` / `draw_assist` / `draw_steal` / `draw_block` consume one uniform each from their own. An engine that stops drawing blocks must not change which player gets the steals; `tests/test_attribution.py::test_c_drawing_one_credit_does_not_move_another_credits_sequence` pins it. The binary and its choice share one family and one counter and the binary uniform is drawn first, so a game with no steals consumes exactly as many uniforms as it had turnovers. The trainer's re-attribution path uses `(seed, game_id, family, ordinal)` keys -- the fix `usage` needed after keying on the game alone collapsed a whole team-game onto one player |
| The L4 attribution game-level check is a K-generic PORT of `usage`'s, pinned to it by test | SHIPPED 2026-09-10 | `usage.alloc_cells` and its helpers are fixed at five alternatives, at `alt_*` column names and at the `usage_alloc` family; the assist target has FOUR alternatives and every target has its own family, so the cell machinery is ported over K and over `cand_*` with identical definitions. `tests/test_attribution.py::test_d_game_level_port_matches_usage_on_five_alternatives` asserts the two agree on a five-alternative frame, so a number quoted next to a usage number is the same statistic and not a lookalike. `usage.py` is NOT edited |
| `ENGINE_FG_MAKE` selects the fg_make artifact set | SHIPPED 2026-09-10, backward compatible | Default `winner` is round 1's adopted `winner_FGA_*.joblib` plus `ENGINE_FG3`, byte for byte -- nothing under `data/processed/models/engine/` and no `winner_FGA_*.joblib` was read differently or rewritten. `round2_S_A` .. `round2_S_E` load `data/processed/models/fg_make/round2/<arm>/`, and an unknown value raises rather than falling back. The state block gained FOUR columns (`score_diff_pre`, `gt_flag`, `eg_trail`, `eg_lead`) APPENDED to `adapters.STATE_COLS`, never inserted, because every existing `FeaturePlan` indexes that tuple by position. The engine's margin is already pre-shot, so `score_diff_pre` is the same live value under the name the round-2 arms were trained on; `tests/test_engine.py::test_round2_state_definitions_agree_between_training_and_the_engine` pins the simulated indicator definitions to `fg_make.add_round2_state` bit for bit, and a second test pins the flag's default and its rejection of junk. 15 engine tests and 43 fg_make tests pass |
| `ENGINE_FG_MAKE=round2b_S_C_s1` serves fg_make as a DATED S1 SCHEDULE through `cbb_sim.engine.manifest` | SHIPPED 2026-09-10 (wired, adopted, NOT the default) | `FgMakeAdapter._load_round2b` loads one `ArtifactManifest` per shot class from `data/processed/models/fg_make/round2b/S_C_s1/manifest_<class>.json` with `require_max_train_date=True`, so both honest-backtest checks (`refit_date < tipoff` and `max_train_date < game_date`) are enforced by the same code path the event layer uses. `FgMakeAdapter.predict` gained an optional `gidx` argument and does one batched predict PER REFIT SEGMENT over disjoint row sets -- the same total row count, still no per-game model call, `CLAUDE.md`'s no-live-calls rule intact. Throughput unchanged within noise (3,125 poss/s against 2,956-3,350 for the static arms on the same 4 workers). fg_make is the SECOND sub-model to arrive as a schedule, so `manifest.py`'s claim to be generic is now exercised by two callers rather than one. `scheme_static_fg_make` flips to False the moment the PM switches the default |
| L4 rotation Decision-10 closed-loop gate (the incumbent R2 has never had one, L25) | **RUN AND PASSED 2026-09-11 for BOTH R2 and the round-4 H1 arm** | New `ENGINE_ROTATION_FREEZE=1` path in `engine/rotation_adapter.py` + `engine/loop.py` holds the rotation model's margin at its pregame value (0) and its personal- and team-foul counts at theirs, while leaving foul accrual, the foul-out eviction rule and the box-score foul counters live. Paired-stream runs, 5 seeds over a fixed 500-game subset of F2 2025 (slate sorted by `game_id`, every 11th row, first 500 -- the same subset the clock round-3c check uses), with `ENGINE_EVENT=round2_s1`, `ENGINE_FG_MAKE=round2b_S_C_s1`, `ENGINE_FG3=decision8`, `ENGINE_CLOCK=reference` pinned and recorded in every `run_meta.json`. R2: margin SD 16.2915 live vs 16.3735 frozen (**ratio 0.9950**), home/away correlation 0.0517 vs 0.0543 (-0.0026), possessions 71.670 vs 71.609 (+0.061), per-player minutes MAE 9.332 vs 9.503. H1: 16.4533 vs 16.5148 (**0.9963**), 0.0437 vs 0.0331 (+0.0106), 71.674 vs 71.580 (+0.094), MAE 8.774 vs 8.840. Two orders of magnitude smaller than L23's `fg_make` `score_diff` loop (margin SD 34.6 live vs 11.4 frozen): **the rotation changes who scores, not how much, so it cannot inflate its own input.** The engine table also reproduces the offline MAE ordering independently (H1 8.774 vs R2 9.332). Engine cost of the round-4 family +32% on the rotation layer (500 games x 1 seed, 123 s vs 93 s). `results/engine_v0/rot4_{R2,H1}_{live,frozen}/`, `experiments.md` section 11.9 |

## D. Data repairs & data defects

| item | status | evidence |
|---|---|---|
| CBBD `/plays/date` returns 18-19% exactly duplicated rows (a game straddling local midnight is pulled twice), touching 21-25% of games | FIXED IN THE LOADER 2026-09-10 | `cbb_sim.pbp.events.load_plays` de-duplicates on `(gameId, id)`. 449,518 of 2,310,520 rows in 2022 ... 626,041 of 3,537,141 in 2026. The playType counts in `docs/tests/data_audit_cbbd_pbp_2026-09-10.md` are pre-de-duplication and are inflated by that factor |
| CBBD per-row `isHomeTeam` is inverted on a handful of games per season (5 of 6,135 in 2025) | FIXED IN THE LOADER 2026-09-10 | `cbb_sim.pbp.possessions._fix_flipped_sides` settles it from the running `homeScore`/`awayScore` columns by per-game majority vote. Left unrepaired it attributes an entire team's scoring to the opponent. `tests/test_possession_outcome.py::test_flipped_is_home_flag_is_repaired_from_the_running_score` |
| CBBD pbp is materially incomplete in 2022-2023: the event stream is short of the final score in ~19% of games (2-5 points, i.e. a whole missing possession's worth) | FLAGGED PER GAME 2026-09-10; the followup is done | `docs/tests/possessions_build_2026-09-10.md` section 2 found it; `pbp_complete` now identifies the games. Share of D-I non-truncated games that are complete: 78.69 / 79.89 / 91.81 / 95.36 / 98.00% for 2022-2026. `cbb_sim.data.universe.compute_pbp_complete`, new columns `pbp_complete` / `cbbd_pbp_rows` / `cbbd_pts_delta_home` / `cbbd_pts_delta_away` on `games_universe.parquet` (every existing column kept). The L3 round-2 bake-off runs on `pbp_complete` games only |
| The two feed-completeness numbers disagreed: 19.5% incomplete for 2022 (event-sum) vs 3.6% (running score) | RECONCILED 2026-09-10 -- and the proposed cause was refuted | `docs/tests/possessions_build_v2_2026-09-10.md` section 2.1. Technical free throws are NOT the cause: they are counted on both sides of the event-sum check, and the three other bookkeeping candidates (points on rows with no team, `Not Available` rows flagged scoring, FGA flagged scoring but not made) are exactly ZERO rows in every season. The cause is that 2022-2023 are missing scoring ROWS while the running-score COLUMN, carried forward on every later row, still reaches the final score -- so the running-score check cannot see a missing basket and the event-sum check can. Corroborated from a third direction by the -0.38 2PA-per-team-game bias vs hoopR `team_box` in those two seasons only. Both numbers were right; `pbp_complete` uses the strict event-level one because a model that trains on events needs an event-level guarantee |
| Season 2025 mislabels putbacks: the rim share of the attempt after an offensive rebound is 53.9 / 55.0 / 54.5% in 2022-2024, **44.5% in 2025**, and 56.3% in 2026 | FIXED IN THE EVENT LAYER 2026-09-10 (was: OPEN -- BLOCKS THE `cont` MODEL) | Diagnosis: `docs/tests/possessions_build_2026-09-10.md` section 3.1 and `docs/tests/shot_classification_diag_2026-09-10.md`. ESPN moved `LayUpShot` putbacks into `TipShot` (still rim) and, in 2025 only, into two-point `JumpShot`. It is an upstream vendor defect, not a CBBD or ETL bug: hoopR's independently-built parse of the same games carries the identical wrong tag on 99.6% of the identical plays. Neither `shot_range` (generated in lockstep with `playType`) nor `playText` can catch it; shot LOCATION can, and those rows sit on the same canned at-the-rim placeholder pixel legitimate `TipShot` rows use. FIX: the rim-location override in `cbb_sim.pbp.events` -- a row already resolved to `FGA_jump2` whose release point is within 2.27 ft of a basket becomes `FGA_rim`. The threshold is DERIVED (the pooled 50th percentile of `DunkShot` release distance) and chosen from a ladder of eight stated quantiles by a rule fixed before the rungs were measured. Effect: 2025's continuation rim share 31.63 -> 38.13% against a clean-season band of 38.18-39.38, with the worst clean-season move 0.34 pp against a 0.5 pp over-reach gate. Ladder and per-season effects: `docs/tests/possessions_build_v2_2026-09-10.md` section 3.1. Tables: `data/processed/possessions_v2/`; v1 is left untouched |
| Possession tables are now versioned; v1 is frozen | SHIPPED 2026-09-10 | `cbb_sim.pbp.possessions.POSSESSION_VERSIONS` / `possessions_dir()`; `DEFAULT_POSSESSION_VERSION` stays `v1` until the PM switches it in that one place. `scripts/build_possessions.py --version` defaults to v2 so a bare re-run cannot overwrite the table other workers are reading. Chance rows now also carry their own `fga_rim` / `fgm_rim` / `fga_jump2` / `fgm_jump2` / `fga_3` / `fgm_3`, which is what makes a first-chance-only style rate computable. Possession and chance BOUNDARIES are bit-identical between v1 and v2 by construction (the override only ever turns an `FGA_jump2` into an `FGA_rim`, and the state machine treats those two identically) -- confirmed by identical possession and chance counts per season |
| ~40% of free-throw trips cannot be separated into shooting-foul vs bonus by the feed (no '1 of 2' text; a two-shot trip in the bonus is consistent with both) | OPEN, FLAGGED PER ROW | `ft_trip_ambiguous` on `possessions_{season}.parquet` and `chances_{season}.parquet`; `docs/tests/possessions_build_2026-09-10.md` section 5. The NCAA 7th/10th-foul thresholds themselves are DERIVED from the data and validated, not assumed |
| Rebound opportunities the feed never resolves: no rebound row appears after the miss | FLAGGED PER ROW 2026-09-10 | 12,836 of 1,549,406 opportunities (0.83%), stable at 0.812-0.837% per season. The next event is a loose-ball foul, a shot or a turnover instead of a rebound. Written as `outcome == 'unresolved'` on `data/processed/models/rebound/events_{version}.parquet` and dropped from the target with its count reported -- the same treatment `unknown` gets at L3 round 1, never imputed |
| Dead-ball rebounds are invisible in the possession tables | WORKED AROUND AT THE EVENT LAYER 2026-09-10 | `cbb_sim.pbp.possessions` treats `DeadBallReb` as a no-op by design, so a missed shot recovered at a dead ball is indistinguishable there from one recovered live. The rebound model therefore reads its target off the event stream (`cbb_sim.models.event_stream`), which is also where the `blocked` flag and the free-throw shooter id come from. Measured on 2024, 58.1% of dead-ball rebounds are followed by the next real action from the SHOOTING team, so the class is not a deterministic hand-back to either side either |
| Free-throw shooter id coverage on CBBD pbp | VERIFIED 2026-09-10 | `participant_1_id` is populated on 100.000% of free-throw rows in 2022-2024 and 99.999% in 2025. 8,858 distinct shooters over 2022-2025. Roster POSITION resolves on 79.1% of modelled attempts overall but only 41.98% in 2022 (CBBD rosters exist for 2024-2026 only and are carried back by the stable player id), which is a reported limitation of the position prior, not a silent fallback |
| CBBD leaves the charged player blank on a material share of turnovers | FLAGGED PER ROW 2026-09-10 | `participant_1_id` is present on 99.97-100.00% of FGA and free-throw rows but only 94.94% (2024) and 94.66% (2025) of `Lost Ball Turnover` rows -- team turnovers (shot-clock, ten-second) and steal-promoted turnovers carry no charged player. Those rows cannot enter a who-did-it model and are dropped with the count reported (`usage.coverage_report`), never imputed. Combined with the on-floor gap (L13), the modelled share of credited events is 89.54% for 2024 TOV and 92.76% for 2025 TOV against 93.89-98.41% for every other class. `docs/models/usage/experiments.md` section 2.1, R11 |
| An `and_one` is counted in TWO usage classes by construction | FLAGGED 2026-09-10 | The made field goal is one `FGA_*` credited event and the free-throw trip it draws is a separate `FT_trip` credited event, both credited to the same player. Correct for per-class allocation, but a consumer summing classes to get "possessions used" must know it. `docs/models/usage/model.md` section 9 item 9 |
| CBBD pbp carries **zero** `Substitution` rows in season 2024, ~35 per game in 2025 and ~145 per game in 2026 | OPEN, WORKED AROUND 2026-09-10 | The L4 pre-registration's "fitted on 2024 substitution events" is therefore fitted on substitution events DERIVED from the on-floor stream (a player leaving the on-floor set between consecutive possessions is an exit, entering it is an entry) -- the same event at possession-boundary resolution. Followup: check whether 2026's rows reconcile with on-floor transitions once the seal lifts. `rotation/model.md` section 7 item 2 |
| L4 hazard design matrix carried no foul state: `build_hazard_training` never advanced the foul counter | FIXED IN THE FITTER AND **CLOSED** 2026-09-10 by the round-3 run -- and the corrected arm is WORSE | All five foul coefficients of R5's override pair came back as exactly +0.0000, which is how it was caught. Affects R3 (round 1) and R5, R6 (round 2); **R1, R2, R4 use no hazard and are unaffected** -- R2's round-1 and round-2 numbers agree to four decimals. The real per-possession foul state is now read from the team-game's own `PersonalFoul` events while FITTING (never at simulation time). Re-fitted coefficients and train-season behaviour in `experiments.md` section 5; the corrected full F1 re-run of R5/R6 was cut off by a session stop and is the blocking followup, and round 3 completed it: corrected, R5's blowout band moves from -2.9 pp (a PASS under the defect) to **+6.9 pp** and its `>= 4 fouls` cell from +0.7 to +3.5 pp, so section 4's R5 column flattered the arm on exactly the cells the defect touched. Also exposed a cell passing for the wrong reason: the `>= 4 fouls` cell pools fouled-out players (forced off, share 0) with four-foul players, and the pre-registered "at exactly 4 fouls" diagnostic row shows 0.82 / 0.80 against a real 0.52 |
| CBBD's `participant_1_id` is the ASSISTER, not the shooter, on half of all assisted made field goals | CONFIRMED-DEFECT, WORKED AROUND 2026-09-10 | Measured on 2025 made field goals: `participant_1_id` equals the dedicated `shot_shooter_id` column on 74.5% of rows overall and on only 50.9% of ASSISTED ones, where it is `shot_assisted_by_id` on the other 48.9% -- CBBD's participant ORDER is not stable on a two-participant row. `attribution.build_attr_stream` reads the shooter off `shot_shooter_id` on every field-goal row and uses `participant_1_id` only where it is the sole participant (rebounder, charged player, stealer, blocker). Cost of not doing it, measured: 51% of assisted field goals were unmodellable because the wrong man was removed from the choice set; after the fix, 98.7% are modelled. **Scope beyond this model, now measured**: `docs/tests/shooter_key_audit_2026-09-10.md` audits every sub-model that keys a player off a participant column over 2022-2025. Exactly TWO read the wrong column, and both for the same population (FGA rows): **`usage`** -- FIXED and re-run 2026-09-10 (`event_stream.build_stream(shooter_key=...)`, `usage.build_usage_events` now defaults to `shot_shooter_id`; round-2 winner unchanged on all five classes, section A row above) -- and **`fg_make`** (`models/fg_make.py:361`), which is STILL ON THE WRONG COLUMN and needs a re-run: its whole shooter as-of block accumulates each attempt and its make/miss onto the wrong player on 4.9-14.1% of attempts, so feature set `B_plus_shooter` upward is contaminated. `free_throw`, `rebound`, `rotation` and `attribution` are NOT affected -- FTA rows agree with `shot_shooter_id` on 100.000% of rows in all four seasons, and rebound/turnover/foul/steal/block rows carry no `shot_shooter_id` at all (0.000% populated), so `participant_1_id` is the only and the correct key there. `event_stream`'s `shooter_key` defaults to the OLD column precisely so those four are byte-identical. Also indirectly affected: `scripts/build_engine_inputs.py` lines 81-83 read the round-1 usage artifacts and must be repointed at the `usage_v2` siblings. `docs/models/attribution/features.md` section 1.2 |
| `fg_make` round 3: the round-2b winner (S-C under S1) retrained keyed on `shot_shooter_id` | **RE-RUN COMPLETE 2026-09-10 -- NOT ADOPTED on any class; served model unchanged** | Fixes `fg_make.build_fg_events`/`_season_events` with the same `shooter_key` pattern `usage` used (defaults to `participant_1_id`, byte-identical to every existing artifact; `shot_shooter_id` opt-in). Quantified first on fg_make's own 2022-2025 population (`docs/tests/fg_make_shooter_key_2026-09-10.md`): mismatch confined to assisted made FGAs, 47.7-50.2% (flat by season), 2.4-2.9x larger on rim/three than the jumper; `shooter_make_c` correlation old-vs-new keying is only 0.694/0.802/**0.335** (rim/jumper/three); drop rate for a missing `shot_shooter_id` is 0.04-0.14% pooled (never imputed, reported by season and team). Pre-registered `experiments.md` section 17 (committed fa7c982 BEFORE the retrain ran); results section 18. **F2 log loss is WORSE than the round-2b reference by 51.6 / 46.5 / **8,182.6** noise floors** (rim/jumper/three; floor = second-seed refit, 6e-6 to 8e-5, one to two orders smaller than round 1's own bootstrap floors but the deltas clear even those by 5-80x); Decision 8 flips PASS -> FAIL on the jumper and the three. **This is the finding, not a failure**: the `shooter_make_c` Decision-8 quintile span collapses 16.1/8.1/**35.9**pp (reference) -> 10.2/4.3/**2.8**pp (corrected) -- a 96% collapse on `FGA_3` -- because a 35.9pp shooter-skill spread in three-point make probability was never plausible; it is the size the mislabelling manufactures when ~half of assisted makes are credited to the team's primary ball-handler, whose contaminated history then acts as a proxy for team shot quality. Against round 1's own `team_baseline` (no shooter at all), the shooter block's F2 log-loss value on `FGA_3` was 0.0474 under the defect and is **0.0018 (96% gone) once corrected** -- almost the entire apparent shooter signal for threes was the labelling bug. Rim/jumper shrink by 26%/46%, tracking each class's mismatch rate. Decision-10 closed-loop for the corrected arm is PENDING an `adapters.py` change (`FgMakeAdapter._load_round2b` has no branch for `round3_shooter/`; this worker does not own `adapters.py`); the reference closed-loop is cited from round 2b (section 16.3), not re-run. **Served model unchanged: `ENGINE_FG_MAKE=round2b_S_C_s1`, still keyed on `participant_1_id`.** Open item for round 4: re-run the shooter-block bake-off from scratch on `shot_shooter_id` rather than assume S-C-S1's spec (chosen against the contaminated label) still applies. `docs/models/fg_make/model.md` item 8, `docs/models/fg_make/features.md` `shooter_make_c` row |
| CBBD leaves the rebounder blank on a large share of OFFENSIVE rebounds | FLAGGED PER ROW 2026-09-10 | `participant_1_id` is absent on roughly 18-25% of `Offensive Rebound` rows and about 7% of `Defensive Rebound` rows -- the feed records a TEAM rebound with no player. Those rows cannot enter a who-did-it model and are dropped with the count reported by `attribution.coverage_report`, never imputed -- the same treatment `usage` gives team turnovers. Consequence: `REB_off` trains on about 81% of offensive rebounds, the lowest coverage of the eight attribution targets. `docs/models/attribution/model.md` section 9 item 5 |
| ENGINE v0: possession-level simulator, round-2 possession-outcome winners wired on a generic S1 dated-artifact manifest (`src/cbb_sim/engine/manifest.py`) | BUILT AND GATED 2026-09-10; TWO BLOCKING DEFECTS DIAGNOSED (L23, Decision 10) | 13/13 tests in 3 flag configs. First gradeable run 5,710 x 5 seeds: PASS 2 / FAIL 6 / NEEDS-INSTRUMENTATION 4 (G3/G4/G8 lack truth tables). Total bias +0.014 vs Control -2.826; leak screen PASS; per-possession mix within 1.4 pp; quintile monotone 4/4 but slope ratio 0.555. Defect 1: fg_make `score_diff` feedback (margin SD 34.6 -> 11.4 when frozen; corr(home,away) -0.64 -> +0.42; actual 14.6 / +0.25). Defect 2: clock `score_diff` is the whole +4.24 possession miss (71.6 -> 67.84 frozen vs 67.88 actual). No hand tuning. Seed study: 200 seeds minimum for gates, ~2,000 before any ROI or Brier. Clock lookup table buys 1.27x at 1.65 poss/game MAE, so the throughput target stands unmet and the clock is not 85% of cost. `docs/tests/engine_v0_F2_2026-09-10.md`, `docs/tests/engine_seed_count_2026-09-10.md`, `docs/tests/gates_engine_v0_F2_2025_s5_r2event_2026-09-10.md` |
| G3/G4/G8 truth tables did not exist: no shot-location truth, no make counts by class, no player truth table, so all three gates read NEEDS-INSTRUMENTATION on the truth side | BUILT 2026-09-10, PROVISIONAL (not yet bake-off/second-source-verified as a grading source) | `scripts/build_truth_tables.py` writes `data/processed/truth/{team_game_shots,player_game,game_finals}_v1.parquet` for seasons 2022-2025 (2026 sealed, never read), each with BOTH an event-layer source (CBBD `possessions_v2` / raw pbp keyed on `shot_shooter_id`, never `participant_1_id`) and a box/schedule source (hoopR `team_box`/`player_box`/schedules, CBBD `games`) side by side, plus a diff and disagreement flag per stat -- neither source is picked over the other. Team-level FGA reconciliation (event vs box) climbs 80.3%/81.5%/94.6%/97.3% exact-match 2022-2025, tracking the already-known CBBD feed-completeness curve; FTA/FTM reconciliation is flat at ~90-93% in EVERY season including 2024-2025 where FGA has converged, pointing at the ~40% `ft_trip_ambiguous` free-throw trips rather than feed completeness. Player crosswalk (`cbbd_player_id`) is 0% for 2022-2023 (CBBD rosters only pulled 2024-2026) and 99.8-99.9% for 2024-2025, reported not hidden. Game finals: OT count never disagrees between hoopR and CBBD when both report it, in any season (hoopR is simply null for 124/11/4/5 games/season, which CBBD fills); 2025 has 4 final-score disagreements, one a CBBD 0-point data gap and one a home/away side-flip (same defect class as the pbp `isHomeTeam` flip), the other two off by 2 points -- all four flagged, none resolved. Caught and fixed during the build: an unfiltered join against the raw hoopR box (before restricting to `is_d1_game & ~pbp_truncated`) inflated box-only team-games ~6x; hoopR's `home_linescores` has no comma between dict elements in some seasons (`"[{'value': 36.0} {'value': 39.0}]"`), so `ast.literal_eval` silently returned nothing until replaced with a `'value':` regex (the same technique `universe.py`'s own period-count parser already used, just extended to capture the value). `scripts/eval_gates.py` gained a backward-compatible `--truth-dir` argument (default `data/processed/truth`; falls back to pre-truth-table behaviour byte-for-byte when a season/file is missing, verified by diff); `gates.gate_g3`/`gate_g4`/`gate_g8` now read the tables. Re-run on `results/engine_v0/F2_2025_s5_r2event` (season 2025, 5 seeds), G1/G2/G5/G6/G7/G9 unchanged byte-for-byte: G3 gets three new POWERED season-level pooled reads (three_pa_share/fta_per_fga/rim_share all PASS, rim share -0.56pp -- the first rim-share truth this project has ever had) though by-team cells stay UNDERPOWERED (a pre-existing min_cell_n=300 vs ~30-40 games/team power issue, not a truth-table gap); G4 gets pooled tov_pct (PASS) / oreb_pct (FAIL, -1.29pp) / ft_rate (PASS), eFG% stays NEEDS-INSTRUMENTATION because games.parquet carries attempt counts only and never makes (confirmed by inspecting the actual output file) -- an engine-contract gap the truth table cannot fix; G8 gets its first-ever actual-box comparison: rotation minutes mean PASSES (29.93 vs 29.83) but minutes SD ratio FAILS (1.22 vs 0.9-1.1) and players-used-per-team-game FAILS (8.81 vs 9.80, K-S p~0) -- independent corroboration of the already-documented usage under-concentration defect (row above, L4 usage bake-off), not a new finding. `docs/tests/truth_tables_v1_2026-09-10.md`, `docs/tests/gates_engine_v0_F2_2025_s5_r2event_PROVISIONAL_2026-09-10.md` |
| Results-contract FGM/FTM extension: `games.parquet` gained `fgm3, fgm2_rim, fgm2_jump, ftm` (home/away pairs) and `players.parquet` gained `fgm2_rim, fgm2_jump, fgm3`, closing the G4 eFG% engine-contract gap the row above left open | SHIPPED 2026-09-10 | `src/cbb_sim/eval/contract.py` `BOX_STATS` extended (append-only, `REQUIRED_GAME_COLUMNS`/`REQUIRED_PLAYER_COLUMNS` untouched); `src/cbb_sim/engine/state.py` mirrors the same `BOX_STATS`/`PLAYER_STATS` tuples so `new_state`/`_finalise` provision the arrays and columns automatically; `src/cbb_sim/engine/loop.py` increments the make counters at the point each attempt's outcome is already drawn (no new RNG draws added). `gate_g4` (`src/cbb_sim/eval/gates.py`) now computes eFG% by team and season-pooled against `team_game_shots_v1.parquet`'s `box_fgm/box_fga/box_fgm3` when both the run and `--truth-dir` support it, and still reports NEEDS-INSTRUMENTATION (with a stated reason) for a results directory missing the new columns or a season without the truth table -- verified byte-for-byte backward compatible by construction, not just by inspection. Identity `points == 2*(fgm2_rim+fgm2_jump) + 3*fgm3 + ftm` added to `tests/test_engine.py::test_score_equals_points_from_events` alongside the existing team-score == player-points and team-FGA == player-FGA identities; per-class player FGM sums to team FGM too. Full suite: `pytest tests/test_engine.py tests/test_eval.py -q` 37/37 pass. 60-game x 5-seed smoke (`results/engine_v0/F2_2025_s5_contract_smoke`, same adapter flags/possession count as the existing `F2_2025_s5_r2event` line of runs) shows the new columns present and `gate_g4` eFG% reading a real number for the first time: pooled 0.4742 (sim) vs 0.5086 (actual, PROVISIONAL truth) -- **FAIL**, not investigated further here (an fg_make/possession-mix question, `model.md` section 7, not an output-contract one); by-team cells UNDERPOWERED as expected at 60 games (~30-40 games/team vs min_cell_n=300, same power issue as G3/G4's other by-team cells). Parity check restricted to the pre-existing columns only, because the committed `docs/ops/parity_reference_windows.json` predates an unrelated adapter-default change (`ENGINE_EVENT` `round2_s1` -> `reference`, plus a new `ENGINE_FG_MAKE` flag) made by other in-flight work, so a full digest compare against it mismatches on old columns too for reasons that have nothing to do with this change (confirmed: an unmodified-HEAD baseline run reproduces the identical large mismatch against that reference). The relevant comparison is a paired same-seed A/B on the SAME current code, contract change on vs off: 0 of 300 game rows and 0 of 4,880 player rows differ on any pre-existing column. `docs/models/engine/model.md` section 1.2 |
| G3/G4/G8 truth tables v2: technical free throws (L24) folded into the FTA/FTM/points reconciliation as explicit `fta_tech`/`ftm_tech` columns, and the four 2025 `game_finals` third-source resolutions applied | BUILT 2026-09-10, PROVISIONAL (same footing as v1; not a bake-off decision) | `scripts/build_truth_tables_v2.py` writes `data/processed/truth/{team_game_shots,player_game,game_finals}_v2.parquet` (2022-2025, 2026 sealed), importing v1's team/box/player/finals builders directly rather than re-deriving them. `fta_tech`/`ftm_tech` are identified the same way `cbb_sim.pbp.possessions._handle_technical` + `_collect_trip` identify a technical FT trip (a `technical` row immediately followed, allowing an administrative OREB/DeadBallReb skip, by a run of FT rows on one team). Netting them out of the event-vs-box FTA comparison lifts exact-match rate from a flat 88.9-92.1% (all four seasons) to 99.25-99.76% -- clears the pre-registered ">97%" bar by a wide margin and quantitatively confirms L24's 94.3%-explained finding at the full-table level. FTM exact-match rises to 99.58-99.93%. A naive points identity (2*2PM+3*3PM+FTM vs hoopR `team_box.team_score`) drops mismatch from 8.5-20.1% to 1.6-12.6% once `tech_ftm` is added, matching `ft_trip_reconciliation`'s independently-computed section-3 numbers exactly (12.6/12.0/4.0/1.6%) -- two independent implementations of the technical-FT walk agree. Residual disagreement after netting is small (0.25-0.74% of matched team-games/season) and split two ways (tech trip found but count still off vs no tech trip found at all), coarser than `ft_trip_reconciliation`'s 5-way waterfall by design (not re-derived here). `player_game_v2` adds the same per-player `fta_tech`/`ftm_tech` keyed on `shot_shooter_id`/`cbbd_player_id` for 2024-2025 (999 / 939 player-games with `fta_tech > 0`); null, not zero, for 2022-2023 (no roster crosswalk, same convention v1 uses for `ev_fga`). `game_finals_v2` applies the four 2025 resolutions from `data/processed/truth/diag_finals_resolution_2025.json` (hoopR right on three, CBBD right on 401722537 where hoopR carried the side-flip) to `home_score`/`away_score`, adds `finals_source`/`finals_third_source_checked`/`finals_resolution_note`; all 22,410 non-flagged rows verified byte-identical to v1 (columns, dtypes, values). `src/cbb_sim/eval/reference.py`'s `load_team_shot_truth`/`load_player_game_truth`/`load_game_finals_truth` gained a `version` kwarg defaulting to `"v2"` (positional `(season, truth_dir)` signature unchanged, so `gates.py`'s existing calls are unaffected); pass `version="v1"` for the frozen original. `gates.py`/`eval_gates.py` NOT touched (v2 is a strict column superset of v1, in flight with the G4 eFG% contract work). `pytest tests/test_eval.py -q`: 22/22 pass, no regressions from the loader default swap (a full `tests/` run was started, found slow under this session's concurrent worker load, and stopped rather than tie up shared compute outside this task's scope). `docs/tests/truth_tables_v2_2026-09-10.md` |
| L3 FG-make's `score_diff` is read off the feed's running score on the attempt's OWN row, and that column is POST-play | **CONFIRMED-DEFECT 2026-09-10, repaired at the source in round 2** | A made three already carries its own three points on the row that records it, so round 1's `C_plus_state` state block contained a post-outcome column in the same family as the `blocked` and `and_one` flags this model bans by name. Proved three ways off the raw feed, none of them using the model: the playText of a made three whose `homeScore` is already 3; the shooting team's own-row score move equalling the shot's value on **94.5 / 94.3 / 93.6 / 93.6%** of made field goals in 2022-2025 against **99.5%** of misses moving by zero; and the collapse of the effect when the shot's own points are removed. It manufactures **62.0% (rim) / 83.3% (jumper) / 83.5% (three)** of the apparent margin effect (raw bucket span 20.12 -> 7.64, 16.21 -> 2.70, 21.64 -> 3.56 pp). The ENGINE always fed the correct pre-shot margin, so round 1 also shipped a train/serve skew. REPAIR: `fg_make.add_round2_state` builds `score_diff_pre` = the margin minus the attempt's own points when it went in (a miss is unchanged by construction, so the correction can only remove outcome information). `score_diff` is NOT added to `BANNED_FEATURES` because arm S-A has to reproduce round 1's numbers; the ban is a PM call. `docs/tests/fg_make_state_confound_2026-09-10.md`, `fg_make/experiments.md` sections 13-14 |
| `cbb_sim.models.attribution` builds `score_diff` from `hs - as_` on the candidate's own row -- the same construction as the fg_make defect above | CONFIRMED-DEFECT, FIXED 2026-09-10 (attribution round 2) -- reaches exactly TWO of eight targets | Quantified with the same own-row delta test L27 used on fg_make, off the raw feed, before any model was touched: of the five populations `build_attr_events` constructs, only `made_fga` (behind `assist`/`assisted`) is a scoring play on its own row (100.0% of its rows move by exactly the shot's point value, both seasons), so it is the ONLY population the leak reaches; `REB_off`/`REB_def`/`steal`(`tov`)/`block`/`miss_fga` are >= 99.9% zero-delta and provably untouched. Manufactures 18.6% of `assisted`'s raw outcome-rate decile span (12.312 -> 10.017 pp) and a 1.3-9.9% shift on `assist`'s two `score_diff` conditional-logit interaction terms. FIX: `attribution.py`'s `_state_block`/`build_attr_events` gained a `score_diff_mode` parameter (`"pre_play"` new default -- subtracts the shot's own points, 2 or 3, on `made_fga` rows only; `"leaked"` selectable for reproduction), the same repair `fg_make.add_round2_state`'s `score_diff_pre` already shipped. Re-run under the fix (F1 selection + within-2025 robustness, round-1's arm class fixed per target, static-fixed vs S1-monthly-fixed): the six untouched targets reproduce round 1's leaked F1 log loss to the 6th decimal (confirming the isolation rather than assuming it); `REB_def` and `block` move to `s1_fixed` (L21's calibration-not-log-loss pattern, a fourth sub-model); `assisted`'s within-2025 calibration miss narrows 6.081 -> 4.331 pp under S1 (a 29% reduction) but still fails the 2.00 pp gate and S1 fails its own F1 gate too, so round 1's `aware_ridge` ships unchanged (bit-identical F1 number) -- the fix barely moves `assisted`'s OWN fitted numbers despite touching 100% of its rows because the correction is an exact affine function of two features (`score_diff`, `sc_three`) the ridge arm already carries, so the fit re-absorbs almost all of it; a real marginal leak and a near-fully-recoverable conditional one are not a contradiction. Two bugs found and fixed while running this round, neither in shipped `attribution.py` code: a joblib pickling defect in the new S1 persistence path (`LgbmChoiceArm`'s custom objective is an unpicklable closure; fixed by persisting the booster's text dump instead of the wrapper) and a decision-eligibility misread in the round-2 script itself (`binary_game_level_check` carries an `sd_pass` key too, so sniffing for it mis-routed every binary target into the choice branch; fixed by branching on the target's own kind, recomputed from already-saved scores, no retraining). `docs/tests/attribution_score_diff_leak_2026-09-10.md`, `docs/models/attribution/experiments.md` sections 4-5, `docs/models/attribution/model.md` section 4.4 |
| Engine default `ENGINE_FG_MAKE` switched `winner` -> `round2b_S_C_s1` | SHIPPED 2026-09-10 (PM) | Round-1 winner carried a post-outcome `score_diff` leak (L27). Round 2b's S-C (engine-safe state, S1 monthly artifacts) passed offline gates and the Decision-10 closed-loop on the 500-game subset: margin SD 14.0 (leaked arm 35.4; actual 12.1), corr(home, away) +0.23 (leaked -0.61; actual +0.42), eFG% 50.15 (leaked 48.63; actual 50.24). `winner` remains selectable for reproduction. Parity reference digest in `docs/ops/parity_reference_windows.json` predates this default and must be regenerated with flags recorded. Round 3 (shooter re-keyed on `shot_shooter_id`) is running and will supersede. |
| CBBD betting lines (ESPN BET 2023-2025) ACCEPTED as the free lines source | DECIDED 2026-09-10 (PM, per user 21:40 EDT) | `docs/tests/lines_cbbd_validation_2026-09-10.md` + addendum. Coverage 99.8/92.6/94.3% of D-I games; overround 4.63% mean; no duplicate rows; home/away sign agrees with verified finals on 100%; 20-bucket implied-vs-realised home win rate tracks the diagonal; spread-implied margin MAE 8.81 vs an honest as-of KenPom-snapshot approximation at 9.00 on the same 16,076 games (corr 0.966); close beats open by 0.06 pts on the 1,802 2025 games with an open (2023-24 have no opens). The gate-reference "10-11 pt" market MAE band was a CFB-derived assumption and is wrong for CBB; the measured band is 8.7-8.9. Limits: no line-level timestamp, so CLV is open-to-close only and only on 2025; `created_at < tipoff` applies to our prediction rows and is unaffected. `data/processed/lines/lines_close_v1.parquet` (100% join to game_id). 2026 lines pulled to data/raw and sealed. |
| Market scorecard (G10) rebuilt to read the ACCEPTED lines source (`lines_close_v1.parquet`) instead of the raw per-season CBBD pull, with a seed-count guard wired in for the first time | SHIPPED 2026-09-10, PIPELINE PROVEN AT 5 SEEDS (no ROI/Brier number adopted yet -- awaiting a >=2,000-seed run) | `scripts/grade_market_games_v2.py` (v1 untouched). Four differences from v1: (1) reads `run_meta.json`'s seed count and, per `docs/tests/engine_seed_count_2026-09-10.md`, labels every table PROVISIONAL below 200 seeds and REFUSES to print any ROI/Brier/calibration number below ~2,000 (prints the seed count and threshold instead, rather than the struck-after-the-fact numbers v1's same-day 5-seed run printed); (2) reads lines from `data/processed/lines/lines_close_v1.parquet` (ESPN BET, proportional de-vig primary, power alongside) instead of `data/raw/cbbd/lines_{season}.parquet`; (3) actual scores come from `game_finals_v2.parquet` ONLY (never `games_universe`'s own score columns, never the engine's own totals) -- games flagged as an unresolved cross-source disagreement are excluded and counted (0 of 5,710 in this run); (4) asserts `created_at < tipoff` per row when the results contract carries row-level timestamps, else labels the report "created_at not recorded" (true of every engine_v0 run today, which carries only a run-level `created_at`). New content beyond v1: range-bucketed (not cumulative) edge tables for spread/total/moneyline with a Wilson 95% CI and the seed-count study's SE on the sim mean per cell, `n<100` cells labelled UNDERPOWERED; breakdowns by season/month/conference-vs-non-conference/team quintile of a new grading-only `own_rating` (as-of `off_c - def_c` from `own_ratings_{season}.parquet`, averaged over both teams and quintiled, via `own_ratings.join_as_of`); a 2025-only open-vs-close leak cross-check (edge-at-open vs open-to-close movement, correlation + 2x2), distinct from v1's close-vs-close surprise-correlation leak screen. Proof run `results/engine_v0/F2_2025_s5_r2event` (5 seeds): guard correctly labels the whole report PROVISIONAL and refuses sections 3/4 (probability calibration; edge-bucket ROI); section-2 sim-vs-market point estimates and the section-5 leak cross-check still print (CLV sign agreement 0.5405 on 1,086 moved 2025 lines, matching v1's number on the same game set). The gated ROI/calibration code path was smoke-tested by temporarily zeroing the seed thresholds against the same 5-seed run (not part of the shipped report) and produces well-formed tables with no exceptions; it has not been exercised against real >=2,000-seed data because none exists yet. `docs/tests/market_scorecard_pipeline_2026-09-10.md`, `docs/tests/market_games_v2_engine_v0_F2_2025_s5_r2event_2026-09-10.md` (+ `.json`). |
| fg_make interim serving decision after round 3: corrected-label S-C-S1 (`round3_shooter/S_C_s1/`) replaces round-2b as the served model; round-2b ineligible on data integrity | DECIDED 2026-09-10 (PM; L29) | Round 3's pre-registered rule was mis-specified (log loss across differently labelled test sets); the PM overrides the "not adopted" reading on data-integrity precedence, as round 2 did for the leaked S-A. Open: Decision-8 shooter slope FAIL on jump2 and FGA_3 under the honest label (spans 4.3 / 2.8 pp); closed-loop not yet run (adapter needs a `round3_shooter_` branch; engine rewiring worker adds it). Round 4 re-bakes the shooter block from scratch on `shot_shooter_id`. |
| `ENGINE_FG_MAKE` gains `round3_shooter_*` and `round4_*`, and the engine DEFAULT moves `round2b_S_C_s1` -> `round3_shooter_S_C_s1` | SHIPPED 2026-09-11 | `FgMakeAdapter._load_round2b` generalised to `_load_dated`, driven by a `_FG_DATED_ROUNDS` prefix -> (round directory, trainer) table, so rounds 2b, 3 and 4 share one loader and the round directory is never hard-coded inside it. Every pre-existing `ENGINE_FG_MAKE` value (`winner`, `round2_S_A..S_E`, `round2b_S_C_s1`) resolves to the same files as before; the loader now also reports each manifest's `shooter_key`, defaulting to `participant_1_id` for manifests that predate round 3, which is the honest value for them rather than a guess. The default moved per L29's interim serving decision, which had never been wired, and is now backed by the closed-loop gate below. `FG_SLOT_ALIAS` gained `shooter_shrunk_dev_c` / `prior_season_att_c` -> class-suffixed forms; `_alias` only binds a key when the real column exists in the inputs' own `slot_names`, so both are a no-op against the stock engine inputs. `pytest tests/test_engine.py -q` 15/15 pass |
| fg_make's ENGINE INPUTS carried a `participant_1_id` train/serve skew: the per-slot shooter block is built from `events_v2.parquet` while the served model is trained on `shot_shooter_id` | **CONFIRMED-DEFECT 2026-09-11, closed into a sibling directory** | Measured on the engine's own arrays: `shooter_make_c__three` correlates **0.3315** between the two keyings (`__rim` 0.7133, `__jump2` 0.8135), reproducing the offline audit's 0.335 / 0.694 / 0.802 inside the engine; `prior_season_make_c__three` 0.2354. Team columns are IDENTICAL under both keys -- verified numerically, not asserted, because attempt and make COUNTS are conserved whichever player is credited. Cost, measured in a paired 500-game x 5-seed run of the SAME model against the two input sets: **0.97 pp of engine three-point make rate** (32.414 skewed vs 33.387 corrected; actual 33.483), 0.45 pp of eFG% and 0.86 points of total bias. `scripts/build_engine_inputs_shotshooter.py` rebuilds ONLY the shooter slot columns into `data/processed/models/engine_fgm4/`, copying every other array through byte for byte with an assertion; nothing under `data/processed/models/engine/` is written, so no other worker is disturbed. **Open: folding the fix into `scripts/build_engine_inputs.py` and regenerating the shared inputs is the step that unblocks serving any round-4 arm** (`fg_make/experiments.md` 20.0, 20.7) |
| fg_make round 3's missing Decision-10 closed-loop gate | **RUN AND PASSED 2026-09-11** | The interim served model `round3_shooter_S_C_s1` on the fixed 500-game subset, 5 seeds, paired streams, `ENGINE_EVENT=round2_s1` set explicitly, against a RE-RUN round-2b reference -- re-run and not cited, because the engine has moved since section 16.3 (the reference's own margin SD reads 13.981 now against 14.002 on record). Margin SD 14.054 (d +0.072), corr(home, away) +0.214 (d -0.015), poss/gm 70.866 (d +0.104), total bias +3.907 (d +0.102): **CL1-CL4 all PASS**. eFG% 49.989 vs 50.239 actual. Engine code pinned to commit `6431772`. `fg_make/experiments.md` section 20.0 |
| `fg_make` round 4: the SHOOTER BLOCK re-baked from scratch on `shot_shooter_id` (B0 no shooter / B1 shrunk rate / B2 + prior-season and counts / BR the incumbent block / B3 + shot mix and assisted share / B4 + spacing) | **DECIDED 2026-09-11: B1 `R4_B1_shrunk` WINS; the served default is NOT yet moved, blocked on the shared engine inputs** | Pre-registered `experiments.md` section 19, committed **f30e051** BEFORE the round ran, on model-free evidence (`docs/tests/fg_make_shooter_skill_2026-09-10.md`): the method-of-moments between-shooter SD of make rate is 6.91 / 3.94 / **2.74** pp (rim / jumper / three), so only 47 / 18 / **15%** of the observed spread in shooter season rates is skill; implied `m` = 50.8 / 152.8 / **298.9** attempts; reliability of a three-point as-of rate is **0.077 at 25 attempts and 0.401 at 200**; and the slope of the realised make rate on the raw as-of rate by attempt bucket tracks `n/(n+m)` bucket for bucket. So L29's 2.8 pp `FGA_3` span is **under-shrinkage, not the truth**: true skill implies a 7.66 pp quintile span and the raw rate recovers 37% of it. Result: **B1 -- ONE column, `shooter_shrunk_dev_c` (the as-of rate shrunk to the shooter's own team rate, `m` = 75 / 200 / 300 fitted on FOLD 1 and frozen, one grid step from the moment estimator by a completely different procedure) -- is the only arm passing calibration AND Decision 8 on all three classes.** It moves the Decision-8 shooter slope from 0.283 / 0.289 / 0.305 (B0, no shooter block, flat at the mean against a 10.2 pp realised span) to **1.040 / 0.838 / 0.924**, closing the `FGA_jump2` and `FGA_3` failures L29 left open, and is the only arm monotone in 4 of 4 quintile steps on all three classes. Every richer arm (B2, BR, B3, B4) falls back to 3 of 4 on the jumper and the three; the hypothesis (20.8 item 1, labelled as a hypothesis) is that handing the tree the raw `shooter_att_c` alongside the shrunk rate lets it partially un-shrink. The Decision-8 exemption was applied AS WRITTEN and not stretched: the realised shooter-driver spans are 10.238 / 4.343 / **2.802** pp, all above the 2.0 pp threshold, so no class is exempt on that driver (`def_allow_c` at 1.378 pp on `FGA_3` IS exempt, recorded per driver). Closed loop, 500 games x 5 seeds paired on corrected inputs, engine code pinned to commit `6431772`: **B1 PASSES CL1-CL4 with the best margin SD (13.757 vs 12.127 actual) and home/away correlation (+0.242 vs +0.423) of any fg_make arm to date.** Cost on record: B1 gives up **64.6 noise floors of `FGA_rim` log loss to BR**, which wins that class outright; the adapter resolves ONE round directory for all three classes, so per-class serving is a real open item (20.8 item 2). Noise floor = second-seed refit of B1's whole S1 schedule (4.4e-5 / 1.1e-4 / 1.8e-5); no conclusion changes under round 1's or round 3's floors instead. Leak test on every new feature: change-form abs(corr) 0.000-0.018 against a 0.15 gate. **The engine default STAYS at `round3_shooter_S_C_s1`**: `round4_B1` RAISES against the shared engine inputs because they do not carry `shooter_shrunk_dev_c__{class}`, and writing those into `data/processed/models/engine/` means rewriting an array six other workers are reading (20.7 names the unblocking step). `experiments.md` sections 19-20 |
| A feature can carry a POST-OUTCOME leak in its JOIN COVERAGE while every value it holds is honest: fg_make round 4's `sh_assisted_share` | **CONFIRMED-DEFECT AND REPAIRED 2026-09-11, mid-round, before any adoption** | The first build aggregated the feed's `shot_assisted` flag over MADE field goals only, one row per (game, shooter). A design row therefore matched **if and only if its shooter made at least one field goal in that game**, and the 3.55% that missed fell back to the no-history value 0.0 -- rows whose realised make rate is **exactly 0.00000** by construction (2.50 / 3.46 / 4.62% by class). It was worth **325 / 89 / 795 noise floors** of F2 log loss (rim / jumper / three, B3 leaked vs repaired on the identical fold) and it made B3 the apparent winner on two classes. **Every VALUE was honest and strictly as-of; the pre-registered change-form leak test passed it at abs(corr) <= 0.026; calibration IMPROVED (1.41 vs 2.10 pp on `FGA_3`) and Decision 8 flipped FAIL -> PASS on `FGA_3` under the leak.** No value-based test can see a missingness leak. Repair: build the panel over every ATTEMPT, so a row exists exactly when the design has a row to join it to (coverage 96.45% -> **100.0000%**); the trainer now RAISES above a 0.1% residual miss. The pre-repair reading is kept at `round4/run_report_v1_leaked_assisted.json` and `round4/B3_leaked_assisted/`, `round4/B4_leaked_assisted/` and is INELIGIBLE -- the same precedence round 2 applied to the leaked S-A and L29 to the mislabelled round-3 reference. B0/B1/B2/BR do not carry the feature and were carried forward unchanged, with the trainer asserting each carried arm's shooter block is unchanged. `experiments.md` section 20.2 |
| `engine/loop.py` called `push_lineups()` BEFORE the period/halftime block, so at a period boundary the rotation was handed the PREVIOUS possession's period and clock | FIXED 2026-09-11 | The five that took the floor for the first possession of the second half were chosen with `period = 1, seconds_remaining = 0`, and the R2 adapter's own period-boundary reshuffle fired one possession late. The offline samplers (`run_scheduler`, `rotation_v4.run_sub_hazard`) have always used the possession's own state, so the engine and the L4 bake-off disagreed **exactly at the cell rotation round 4 adds** (the second-half tip). The call now sits after the period block. Blast radius on aggregate engine metrics is about one possession in 137 and the counter-based streams stay aligned (the number of draws per call is unchanged), but it is a behaviour change for every engine run started after the fix: **any paired engine comparison that straddles this commit is invalid and must be re-run.** `engine/loop.py`, `docs/models/rotation/model.md` section 7 item 17 |
| Engine default `ENGINE_CLOCK` -> `v3c_srfloor_P3_s1` (provisional serving choice; `provisional_clock` stays True) | DECIDED 2026-09-11, BLOCKED (PM; L31): the loop's clock call site carries no game index, so a dated S1 schedule cannot be served per game and the adapter correctly refuses; default stays `reference` until the call site is game-indexed (clock round 4 worker). | Clock round 3c adopted nothing (0 of 6 pass the G1 mean), but the cell-based P3+S1 arm halves the incumbent's overshoot (+1.16 vs +2.70 clock-complete; total bias -0.93 vs +2.83; tempo slope inside Decision 8's band, incumbent outside). Serving the best available provisional arm is not an adoption and not hand tuning; `reference` stays selectable. Note: round-3c runs may predate loop.py commit 6431772 (push_lineups order); all arms were on the same loop, so the paired design holds. Round 4 targets the uniform -1.9% duration shortfall. |
